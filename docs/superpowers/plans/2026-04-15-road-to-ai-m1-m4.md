# Road to AI — M1-M4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and ship the Road to AI platform (reusable gamified-curriculum React app) together with the first 15 Track A levels of its flagship AI curriculum to a public GitHub Pages URL at `https://jvloo.github.io/road-to-ai/`.

**Architecture:** Markdown files are the source of truth for all curriculum content (levels, glossary, meta). A React 19 view layer renders them at runtime (no build-time baking) with a Cytoscape-powered skill tree, Mermaid/KaTeX/Shiki rendering inside level panes, and a one-file `progress.json` for per-user state. The platform directory is domain-agnostic (no hardcoded AI terms); the curriculum directory is the flagship example. Dark mode is the default, sourced from `xavierloo.com`'s visual language. GitHub Pages auto-deploys every push to `main` via GitHub Actions.

**Tech Stack:** React 19, TypeScript (strict), Vite, Tailwind CSS v4, pnpm, Cytoscape.js, Mermaid, KaTeX, Shiki, Framer Motion, Zustand, remark + remark-gfm + remark-math + gray-matter, Lucide React, `@fontsource/*`, Vitest, React Testing Library, Playwright, GitHub Actions.

**Reference spec:** [`docs/superpowers/specs/2026-04-15-road-to-ai-curriculum-and-platform-design.md`](../specs/2026-04-15-road-to-ai-curriculum-and-platform-design.md)

**Prerequisites on the engineer's machine:**
- Node.js ≥ 20 (check with `node -v`)
- pnpm ≥ 9 (`npm i -g pnpm`)
- Git ≥ 2.40
- A GitHub account with `jvloo` access (or the equivalent for a forker)
- GitHub CLI `gh` authenticated (`gh auth status`)

**Global authorship convention (playful, intentional):**

Road to AI is authored by **Prof. Claude** with **Xavier Loo** as co-author and first learner. This is a deliberate inversion of conventional authorship to acknowledge the AI-assisted arrangement. Apply this consistently across every artifact:

- **Git commits:** pass `--author="Prof. Claude <noreply@anthropic.com>"` on every `git commit`. Committer stays Xavier (local git config). Example:
  ```bash
  git -c commit.gpgsign=false commit \
    --author="Prof. Claude <noreply@anthropic.com>" \
    -m "$(cat <<'EOF'
  <subject>

  <body>

  Co-Authored-By: Xavier Loo (first learner) <ping@xavierloo.com>
  EOF
  )"
  ```
- **Commit message trailer:** every commit ends with:
  ```
  Co-Authored-By: Xavier Loo (first learner) <ping@xavierloo.com>
  ```
- **README attribution line:** "By Prof. Claude, with Xavier Loo as co-author and first learner."
- **Copyright line:** remains `Copyright (c) 2026 Xavier Loo` (legal ownership sits with Xavier; authorship credit is playful).
- **Every commit example in this plan shows the old trailer for brevity** — when executing, replace each trailer with the convention above and add the `--author=...` flag. Do NOT rewrite pre-existing commits; this applies to every new commit from Task 1 forward.

`git config user.name` is `Xavier Loo`, `user.email` is `ping@xavierloo.com` (already set at `D:/Personal/Assistant/Learn/.git/config`).

---

## M1 — Scaffold & live site

**Outcome:** A public URL at `https://jvloo.github.io/road-to-ai/` serving a placeholder landing page styled in the xavierloo.com idiom.

### Task 1: Root repository metadata

**Files:**
- Create: `README.md`
- Create: `LICENSE` (MIT)
- Create: `LICENSE-CONTENT` (CC-BY-SA 4.0)
- Create: `CONTRIBUTING.md`
- Create: `CODE_OF_CONDUCT.md`
- Create: `CHANGELOG.md`
- Modify: `.gitignore` (extend with node_modules, dist, platform-specific paths)

- [ ] **Step 1: Extend `.gitignore` with platform and build artifacts**

Append to the existing `.gitignore`:

```gitignore
# Node
node_modules/
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*

# Build output
dist/
build/
.cache/
.turbo/
.vite/

# Env / local
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Per-user progress (gitignored so forkers keep theirs private)
progress.json

# Per-user study artifacts
notes/
notebooks/
papers/
```

- [ ] **Step 2: Write root `README.md`**

```markdown
# Road to AI

> A gamified, open-source path from math to world models — fork it and make your own.

**Road to AI** is two things:

1. A **reusable React platform** that renders any markdown-based gamified curriculum as an interactive skill tree with levels, XP, achievements, and spaced-recall. Domain-agnostic: fork it, drop in your own `curriculum/` folder, and teach anything.
2. A **flagship AI curriculum** — a cold-start-friendly, foundations-first path through classical AI, ML, Deep Learning, Reinforcement Learning, LLMs, Agentic AI, and World Models, with explicit lineage connecting classical ideas to their modern descendants.

Live app: https://jvloo.github.io/road-to-ai/

## Quick start (learner)

Visit the live app. No install required. Your progress lives in your browser (`localStorage`).

## Quick start (fork your own curriculum)

```bash
git clone https://github.com/jvloo/road-to-ai.git my-curriculum
cd my-curriculum
pnpm install
pnpm dev
```

Replace the contents of `curriculum/` with your own levels (markdown with frontmatter — see [`curriculum/README.md`](curriculum/README.md) for the format) and push to your own GitHub Pages site.

## Repo layout

```
platform/     # React app (MIT)
curriculum/   # Flagship AI curriculum (CC-BY-SA 4.0)
docs/         # Design spec and implementation plan
.claude/      # Slash commands for Claude Code users
```

## Design philosophy

- **Markdown is the source of truth.** The app reads; it never owns.
- **No time pressure.** No streaks, no daily quotas, no decay. Levels wait for you.
- **Classical → modern.** Every lesson links old ideas to their living descendants.
- **Dark mode by default.** SWEs love dark mode.

## License

- Platform code (`platform/`): [MIT](LICENSE)
- Curriculum content (`curriculum/`): [CC-BY-SA 4.0](LICENSE-CONTENT)

## Authors

By **Prof. Claude**, with **Xavier Loo** as co-author and first learner.

- Prof. Claude — Anthropic's Claude, primary author of the curriculum and platform code
- Xavier Loo — co-author, first learner, and the one who decided to publicize this — [xavierloo.com](https://xavierloo.com)

*Copyright © 2026 Xavier Loo. Playful authorship credit above; legal ownership rests with Xavier.*
```

- [ ] **Step 3: Write `LICENSE` (MIT)**

Use the standard MIT license text with:
- Copyright line: `Copyright (c) 2026 Xavier Loo`

(Full MIT template — copy verbatim from https://opensource.org/license/mit and update copyright line.)

- [ ] **Step 4: Write `LICENSE-CONTENT` (CC-BY-SA 4.0)**

Use the standard CC-BY-SA 4.0 Legal Code, preceded by:
```
The curriculum content under `curriculum/` is licensed under
Creative Commons Attribution-ShareAlike 4.0 International.

(c) 2026 Xavier Loo
```
Then the full CC-BY-SA 4.0 text from https://creativecommons.org/licenses/by-sa/4.0/legalcode.txt

- [ ] **Step 5: Write `CONTRIBUTING.md`**

```markdown
# Contributing to Road to AI

Contributions welcome across three lanes:

## 1. Platform (`platform/`)

Code improvements, accessibility fixes, performance, new UI features that serve every fork. See [docs/superpowers/specs/](docs/superpowers/specs/) for the design.

Requirements:
- TypeScript strict mode passes (`pnpm typecheck`)
- All tests pass (`pnpm test`)
- Domain-agnostic: no AI-specific terms in `platform/src/` (CI enforces)

## 2. Curriculum (`curriculum/`)

AI-curriculum-specific: new levels, corrections, luminary spotlights, glossary entries.

Requirements:
- Each level file has populated frontmatter (id, title, track, tier, xp, prereqs, unlocks)
- Each level has a "Why this level matters (lineage)" section
- Glossary entries are ≤3 sentences

## 3. A new curriculum for a different subject

Fork the repo and replace `curriculum/`. We'd love to feature great forks in the README — open an issue.

## Commit style

Conventional commits (`feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:`).
```

- [ ] **Step 6: Write `CODE_OF_CONDUCT.md`**

Use the Contributor Covenant v2.1 verbatim (https://www.contributor-covenant.org/version/2/1/code_of_conduct/), with contact email `ping@xavierloo.com`.

- [ ] **Step 7: Write `CHANGELOG.md`**

```markdown
# Changelog

All notable changes to Road to AI are documented here. Format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versioning
follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial project structure (M1 scaffold).
```

- [ ] **Step 8: Commit**

```bash
cd "D:/Personal/Assistant/Learn"
git add README.md LICENSE LICENSE-CONTENT CONTRIBUTING.md CODE_OF_CONDUCT.md CHANGELOG.md .gitignore
git -c commit.gpgsign=false commit \
  --author="Prof. Claude <noreply@anthropic.com>" \
  -m "$(cat <<'EOF'
chore: add repo metadata (README, licenses, contributing, changelog)

MIT covers the platform; CC-BY-SA 4.0 covers curriculum content.
Extends .gitignore with node/build/OS/IDE and per-user artifacts.

Co-Authored-By: Xavier Loo (first learner) <ping@xavierloo.com>
EOF
)"
```

*Note:* every subsequent `git commit` in this plan follows the same pattern — add `--author="Prof. Claude <noreply@anthropic.com>"` and replace the co-author trailer with `Co-Authored-By: Xavier Loo (first learner) <ping@xavierloo.com>`. The other commit examples in this plan still show the older trailer for brevity; apply the convention when executing.

Expected: `[main <hash>] chore: add repo metadata...` with 7 files changed.

---

### Task 2: Platform scaffold with Vite + React 19 + TS + Tailwind v4

**Files:**
- Create: `platform/package.json`
- Create: `platform/pnpm-workspace.yaml` (at repo root — pnpm workspace)
- Create: `pnpm-workspace.yaml` at repo root
- Create: `platform/tsconfig.json`
- Create: `platform/tsconfig.node.json`
- Create: `platform/vite.config.ts`
- Create: `platform/tailwind.config.ts`
- Create: `platform/postcss.config.js`
- Create: `platform/index.html`
- Create: `platform/src/main.tsx`
- Create: `platform/src/App.tsx`
- Create: `platform/src/styles/globals.css`
- Create: `platform/vitest.config.ts`
- Create: `platform/src/setupTests.ts`

- [ ] **Step 1: Create pnpm workspace config at repo root**

File: `pnpm-workspace.yaml`
```yaml
packages:
  - "platform"
```

- [ ] **Step 2: Initialize `platform/package.json`**

File: `platform/package.json`
```json
{
  "name": "@road-to-ai/platform",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router": "^7.0.0",
    "zustand": "^5.0.0",
    "cytoscape": "^3.30.0",
    "mermaid": "^11.4.0",
    "katex": "^0.16.11",
    "shiki": "^1.24.0",
    "framer-motion": "^12.0.0",
    "lucide-react": "^0.468.0",
    "remark": "^15.0.0",
    "remark-gfm": "^4.0.0",
    "remark-math": "^6.0.0",
    "remark-parse": "^11.0.0",
    "rehype-katex": "^7.0.0",
    "rehype-stringify": "^10.0.0",
    "unified": "^11.0.0",
    "gray-matter": "^4.0.3",
    "@fontsource/inter": "^5.1.0",
    "@fontsource/jetbrains-mono": "^5.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/cytoscape": "^3.21.0",
    "@types/katex": "^0.16.7",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "typescript": "^5.7.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "postcss": "^8.4.49",
    "autoprefixer": "^10.4.20",
    "vitest": "^2.1.8",
    "@vitest/ui": "^2.1.8",
    "jsdom": "^25.0.1",
    "@testing-library/react": "^16.1.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/user-event": "^14.5.2",
    "@playwright/test": "^1.49.0",
    "eslint": "^9.17.0",
    "@eslint/js": "^9.17.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.4.16",
    "typescript-eslint": "^8.18.0"
  },
  "packageManager": "pnpm@9.15.0"
}
```

- [ ] **Step 3: Write `platform/tsconfig.json`**

File: `platform/tsconfig.json`
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["vitest/globals", "@testing-library/jest-dom"]
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **Step 4: Write `platform/tsconfig.node.json`**

File: `platform/tsconfig.node.json`
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "noEmit": true
  },
  "include": ["vite.config.ts", "vitest.config.ts", "tailwind.config.ts"]
}
```

- [ ] **Step 5: Write `platform/vite.config.ts`**

File: `platform/vite.config.ts`
```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "node:path";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  base: "/road-to-ai/",
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  server: {
    port: 5173,
  },
  build: {
    outDir: "dist",
    sourcemap: true,
  },
});
```

- [ ] **Step 6: Write `platform/tailwind.config.ts`**

File: `platform/tailwind.config.ts`
```ts
import type { Config } from "tailwindcss";

export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  darkMode: "class",
  theme: {
    extend: {},
  },
  plugins: [],
} satisfies Config;
```

- [ ] **Step 7: Write `platform/postcss.config.js`**

File: `platform/postcss.config.js`
```js
export default {
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 8: Write `platform/index.html`**

File: `platform/index.html`
```html
<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#0a0a0a" media="(prefers-color-scheme: dark)" />
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)" />
    <meta name="description" content="A gamified, open-source path from math to world models." />
    <title>Road to AI</title>
  </head>
  <body class="bg-bg text-fg antialiased">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 9: Write `platform/src/styles/globals.css`**

File: `platform/src/styles/globals.css`
```css
@import "tailwindcss";
@import "@fontsource/inter/latin-400.css";
@import "@fontsource/inter/latin-500.css";
@import "@fontsource/inter/latin-600.css";
@import "@fontsource/inter/latin-700.css";
@import "@fontsource/jetbrains-mono/latin-400.css";
@import "@fontsource/jetbrains-mono/latin-500.css";

@theme {
  /* Placeholders — finalized in Task 3 from xavierloo.com extraction */
  --color-bg: #0a0a0a;
  --color-fg: #f5f5f5;
  --color-muted: #8a8a8a;
  --color-border: #222222;
  --color-accent: #b9ff66;

  --font-sans: "Inter", system-ui, -apple-system, sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, SFMono-Regular, Consolas, monospace;
}

@layer base {
  :root {
    color-scheme: dark;
  }

  html.light {
    --color-bg: #ffffff;
    --color-fg: #0a0a0a;
    --color-muted: #6a6a6a;
    --color-border: #e5e5e5;
    color-scheme: light;
  }

  body {
    font-family: var(--font-sans);
    font-feature-settings: "cv02", "cv03", "cv04", "cv11";
  }

  code,
  kbd,
  samp,
  pre {
    font-family: var(--font-mono);
  }
}
```

- [ ] **Step 10: Write `platform/src/main.tsx`**

File: `platform/src/main.tsx`
```tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App";
import "./styles/globals.css";

const rootEl = document.getElementById("root");
if (!rootEl) throw new Error("Root element #root not found in index.html");

createRoot(rootEl).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 11: Write a placeholder `platform/src/App.tsx`**

File: `platform/src/App.tsx`
```tsx
export default function App() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16">
      <p className="font-mono text-sm text-[var(--color-muted)]">// road-to-ai</p>
      <h1 className="mt-4 text-5xl font-semibold tracking-tight">Road to AI</h1>
      <p className="mt-6 text-lg text-[var(--color-muted)]">
        A gamified, open-source path from math to world models.
      </p>
      <p className="mt-8 font-mono text-xs text-[var(--color-muted)]">
        scaffold online · m1 complete · full app landing in m2
      </p>
    </main>
  );
}
```

- [ ] **Step 12: Write `platform/vitest.config.ts`**

File: `platform/vitest.config.ts`
```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import path from "node:path";

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  test: {
    globals: true,
    environment: "jsdom",
    setupFiles: ["./src/setupTests.ts"],
    css: false,
  },
});
```

- [ ] **Step 13: Write `platform/src/setupTests.ts`**

File: `platform/src/setupTests.ts`
```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 14: Install dependencies**

Run:
```bash
cd "D:/Personal/Assistant/Learn"
pnpm install
```

Expected: pnpm creates `node_modules/` and `pnpm-lock.yaml`, resolves all deps with zero peer warnings about core React/Vite/Tailwind.

- [ ] **Step 15: Run the dev server smoke-check**

Run:
```bash
cd "D:/Personal/Assistant/Learn/platform"
pnpm dev
```

Expected: Vite starts on `http://localhost:5173/road-to-ai/`. Open in browser — see "Road to AI" heading with the `// road-to-ai` code-comment prefix, dark background. Kill the server with Ctrl+C.

- [ ] **Step 16: Run typecheck and build**

Run:
```bash
cd "D:/Personal/Assistant/Learn/platform"
pnpm typecheck && pnpm build
```

Expected: no type errors; `dist/` directory created with `index.html` and bundled assets.

- [ ] **Step 17: Commit**

```bash
cd "D:/Personal/Assistant/Learn"
git add pnpm-workspace.yaml pnpm-lock.yaml platform/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): scaffold React 19 + Vite + TS + Tailwind v4 app

Initializes the domain-agnostic platform with:
- Vite 6 + React 19 + TypeScript (strict) + Tailwind v4 + pnpm workspace
- Vitest + React Testing Library + Playwright configured
- Placeholder landing page using the xavierloo.com code-comment idiom
- Dark mode as the default theme via <html class="dark">

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Extract design tokens from xavierloo.com

**Files:**
- Create: `platform/src/styles/tokens.css`
- Modify: `platform/src/styles/globals.css` (import tokens.css)
- Create: `docs/design-decisions.md`

- [ ] **Step 1: Inspect xavierloo.com for design tokens**

Open `https://xavierloo.com` in a browser. Open DevTools → Elements. Inspect:
1. `body` computed styles — record `background-color`, `color`, `font-family`
2. Heading (`h1`) — record `color`, `font-family`, `font-weight`, `font-size`, `line-height`
3. The "LIVE" badge or any accent-colored element — record its exact color
4. Any `--` CSS custom property exposed on `:root` or `body`
5. A body paragraph — record `color`, `font-size`, `line-height`, `max-width` of container
6. The monospace "// work" / "// projects" divider — record `font-family`, `font-size`, `color`

Record findings in `docs/design-decisions.md` (created in step 3).

- [ ] **Step 2: Write `platform/src/styles/tokens.css` with extracted values**

File: `platform/src/styles/tokens.css`
```css
/*
 * Design tokens extracted from https://xavierloo.com on 2026-04-15.
 * When the source site evolves, re-extract and update these values.
 * Dark mode is the default; light mode mirrors the source site exactly.
 */

@theme {
  /* — DARK (default) — */
  --color-bg: <REPLACE: extracted body bg in dark context or inverted light>;
  --color-fg: <REPLACE: primary text color inverted>;
  --color-muted: <REPLACE: secondary text, darker>;
  --color-border: <REPLACE: subtle border>;
  --color-surface: <REPLACE: card/raised surface>;
  --color-accent: <REPLACE: the LIVE-badge accent>;
  --color-accent-fg: <REPLACE: text color on accent background>;

  /* typography */
  --font-sans: <REPLACE: site body font stack, quoted>;
  --font-mono: <REPLACE: site monospace stack, quoted>;
  --font-display: var(--font-sans);

  /* spacing scale (source site's base unit) */
  --space-0: 0;
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-12: 3rem;
  --space-16: 4rem;

  /* radii */
  --radius-sm: 0.25rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;

  /* shadows (used sparingly) */
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.2);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.3);

  /* motion */
  --duration-fast: 150ms;
  --duration-base: 250ms;
  --duration-slow: 300ms;
  --easing: cubic-bezier(0.2, 0.8, 0.2, 1);
}

html.light {
  --color-bg: <REPLACE: exact site bg>;
  --color-fg: <REPLACE: exact site fg>;
  --color-muted: <REPLACE: secondary>;
  --color-border: <REPLACE: subtle border>;
  --color-surface: <REPLACE: card surface>;
  --color-accent-fg: <REPLACE: text on accent in light mode>;
  /* accent stays the same across themes */
}
```

Replace each `<REPLACE: …>` with the actual value recorded from xavierloo.com in step 1.

- [ ] **Step 3: Write `docs/design-decisions.md`**

File: `docs/design-decisions.md`
```markdown
# Design Decisions

Append a one-line entry whenever a design-level choice is made or revised.
Keep this short — rationale lives in commits or the spec.

| Date       | Decision                                                | Rationale |
|------------|---------------------------------------------------------|-----------|
| 2026-04-15 | Extracted tokens from xavierloo.com, committed to tokens.css | Source of visual identity per spec §6.H |
| 2026-04-15 | Dark mode is default; light mirrors source site          | Per user feedback (SWE audience) |
```

- [ ] **Step 4: Import tokens into globals.css**

Modify `platform/src/styles/globals.css`: replace the inline `@theme { --color-bg: #0a0a0a; ... }` block with:

```css
@import "tailwindcss";
@import "./tokens.css";
@import "@fontsource/inter/latin-400.css";
@import "@fontsource/inter/latin-500.css";
@import "@fontsource/inter/latin-600.css";
@import "@fontsource/inter/latin-700.css";
@import "@fontsource/jetbrains-mono/latin-400.css";
@import "@fontsource/jetbrains-mono/latin-500.css";

@layer base {
  :root { color-scheme: dark; }
  html.light { color-scheme: light; }

  body {
    font-family: var(--font-sans);
    font-feature-settings: "cv02", "cv03", "cv04", "cv11";
    background: var(--color-bg);
    color: var(--color-fg);
  }

  code, kbd, samp, pre {
    font-family: var(--font-mono);
  }
}
```

- [ ] **Step 5: Run dev server and verify visual match**

```bash
cd "D:/Personal/Assistant/Learn/platform"
pnpm dev
```

Open `http://localhost:5173/road-to-ai/` — compare typography and palette to xavierloo.com side by side. Heading weight, line-height, and accent color should read as the same design family. Stop the server.

- [ ] **Step 6: Commit**

```bash
cd "D:/Personal/Assistant/Learn"
git add platform/src/styles/tokens.css platform/src/styles/globals.css docs/design-decisions.md
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): extract design tokens from xavierloo.com

Captures palette, typography, spacing scale, and motion tokens from the
author's personal site to tokens.css so the app is visually consistent
with the source brand. Adds docs/design-decisions.md to track any
future deviations.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Landing page with code-comment dividers

**Files:**
- Modify: `platform/src/App.tsx`
- Create: `platform/src/components/Divider.tsx`
- Create: `platform/src/components/Divider.test.tsx`

- [ ] **Step 1: Write the failing test for `Divider`**

File: `platform/src/components/Divider.test.tsx`
```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { Divider } from "./Divider";

describe("Divider", () => {
  it("renders the label prefixed with // in a monospace style", () => {
    render(<Divider>journey</Divider>);
    const el = screen.getByText(/\/\/\s*journey/);
    expect(el).toBeInTheDocument();
    expect(el.tagName.toLowerCase()).toBe("p");
  });

  it("adds a trailing horizontal rule", () => {
    const { container } = render(<Divider>projects</Divider>);
    expect(container.querySelector("hr")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run the test to confirm it fails**

```bash
cd "D:/Personal/Assistant/Learn/platform"
pnpm test:run src/components/Divider.test.tsx
```

Expected: test fails with "Cannot find module './Divider'".

- [ ] **Step 3: Implement `Divider`**

File: `platform/src/components/Divider.tsx`
```tsx
import type { ReactNode } from "react";

export function Divider({ children }: { children: ReactNode }) {
  return (
    <div className="mt-12 mb-4">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">
        {`// ${children}`}
      </p>
      <hr className="mt-2 border-[var(--color-border)]" />
    </div>
  );
}
```

- [ ] **Step 4: Run the test to verify it passes**

```bash
pnpm test:run src/components/Divider.test.tsx
```

Expected: 2 tests pass.

- [ ] **Step 5: Use `Divider` in the landing page**

Replace `platform/src/App.tsx` with:
```tsx
import { Divider } from "./components/Divider";

export default function App() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16">
      <p className="font-mono text-sm text-[var(--color-muted)]">// road-to-ai</p>
      <h1 className="mt-4 text-5xl font-semibold tracking-tight">Road to AI</h1>
      <p className="mt-6 text-lg text-[var(--color-muted)]">
        A gamified, open-source path from math to world models — fork it and make your own.
      </p>

      <Divider>what this is</Divider>
      <p className="leading-relaxed">
        A reusable React platform that renders any markdown-based gamified curriculum as
        an interactive skill tree with levels, XP, achievements, and spaced recall — plus
        a flagship AI curriculum as the default example.
      </p>

      <Divider>status</Divider>
      <ul className="space-y-2 font-mono text-sm">
        <li>
          <span className="text-[var(--color-accent)]">●</span> m1 scaffold · online
        </li>
        <li className="text-[var(--color-muted)]">○ m2 skill tree · in progress</li>
        <li className="text-[var(--color-muted)]">○ m3 gamification · queued</li>
        <li className="text-[var(--color-muted)]">○ m4 flagship content · queued</li>
      </ul>

      <Divider>links</Divider>
      <ul className="space-y-1 text-sm">
        <li>
          <a className="underline hover:text-[var(--color-accent)]" href="https://github.com/jvloo/road-to-ai">
            GitHub: jvloo/road-to-ai
          </a>
        </li>
        <li>
          <a className="underline hover:text-[var(--color-accent)]" href="https://xavierloo.com">
            Author: Xavier Loo
          </a>
        </li>
      </ul>
    </main>
  );
}
```

- [ ] **Step 6: Run dev server and visually inspect**

```bash
pnpm dev
```

Open `http://localhost:5173/road-to-ai/`. Verify:
- The `// road-to-ai` monospace prefix above the hero
- Four code-comment section dividers visible
- `●` marker in accent color on the active status line
- Link underlines turn accent color on hover

Stop the server.

- [ ] **Step 7: Commit**

```bash
cd "D:/Personal/Assistant/Learn"
git add platform/src/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): landing page with code-comment section dividers

Adds a reusable <Divider> component matching xavierloo.com's
"// section-name" visual pattern. Landing page now shows the mission
statement, M1-M4 progress indicators, and links.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: GitHub Actions workflow for Pages deploy

**Files:**
- Create: `.github/workflows/pages.yml`
- Create: `.github/ISSUE_TEMPLATE/bug_report.md`
- Create: `.github/ISSUE_TEMPLATE/curriculum_correction.md`
- Create: `.github/ISSUE_TEMPLATE/feature_request.md`

- [ ] **Step 1: Write the Pages deploy workflow**

File: `.github/workflows/pages.yml`
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm --filter @road-to-ai/platform typecheck

      - name: Run unit tests
        run: pnpm --filter @road-to-ai/platform test:run

      - name: Build
        run: pnpm --filter @road-to-ai/platform build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: platform/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: Write the three issue templates**

File: `.github/ISSUE_TEMPLATE/bug_report.md`
```markdown
---
name: Bug report
about: Something's broken in the app
labels: bug
---

### What happened?

### What did you expect?

### Reproduction steps

### Browser / OS

### Screenshots (optional)
```

File: `.github/ISSUE_TEMPLATE/curriculum_correction.md`
```markdown
---
name: Curriculum correction
about: Something in a level, luminary, or glossary entry is wrong
labels: curriculum
---

### Which level / glossary entry / luminary?

### What's incorrect?

### Proposed correction

### Source (paper, textbook, url)
```

File: `.github/ISSUE_TEMPLATE/feature_request.md`
```markdown
---
name: Feature request
about: An idea for the platform or curriculum
labels: enhancement
---

### What do you want to be able to do?

### Why does it matter?

### Possible approaches

### Is this platform-level or curriculum-level?
```

- [ ] **Step 3: Commit**

```bash
cd "D:/Personal/Assistant/Learn"
git add .github/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
ci: add GitHub Pages deploy workflow and issue templates

Deploys platform/dist to GitHub Pages on every push to main after
passing typecheck and unit tests. Issue templates for bugs,
curriculum corrections, and feature requests.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: First push to GitHub + verify live deploy

**Files:** none (uses `gh` CLI)

- [ ] **Step 1: Create the public GitHub repo**

```bash
cd "D:/Personal/Assistant/Learn"
gh repo create jvloo/road-to-ai --public --source=. --remote=origin --push --description="A gamified, open-source path from math to world models."
```

Expected: repo created, main pushed. URL in output.

- [ ] **Step 2: Enable GitHub Pages via the API**

```bash
gh api repos/jvloo/road-to-ai/pages -X POST -f source='{"branch":"main","path":"/"}' -f build_type=workflow
```

If the repo setting requires it, enable Pages through the UI: **Settings → Pages → Source: GitHub Actions**.

- [ ] **Step 3: Watch the workflow**

```bash
gh run watch
```

Expected: workflow succeeds. Note the deployed URL from the run output.

- [ ] **Step 4: Smoke-test the live URL**

Open `https://jvloo.github.io/road-to-ai/` in a browser. Verify:
- The landing page renders in dark mode by default
- Typography and accent color match xavierloo.com
- All four code-comment dividers render
- Links are clickable

- [ ] **Step 5: Tag M1 milestone**

```bash
cd "D:/Personal/Assistant/Learn"
git tag -a platform-v0.0.1 -m "M1: scaffold + live placeholder"
git push origin platform-v0.0.1
```

- [ ] **Step 6: Update CHANGELOG**

Modify `CHANGELOG.md`:
```markdown
## [Unreleased]

## [platform-v0.0.1] — 2026-04-15

### Added
- M1 scaffold: Vite + React 19 + TS + Tailwind v4 app live at https://jvloo.github.io/road-to-ai/
- Design tokens extracted from xavierloo.com
- GitHub Pages workflow for auto-deploy on push to main
- Issue templates and contributing guide
```

Commit:
```bash
git add CHANGELOG.md
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
docs: log platform-v0.0.1 M1 milestone in changelog

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

**M1 done.** The URL is live, tokens match xavierloo.com, and the pipeline is in place for every subsequent push.

---

## M2 — Level loader & skill tree

**Outcome:** Click a node in an interactive skill tree, see its level markdown rendered with Mermaid diagrams, KaTeX math, and Shiki code highlighting.

### Task 7: Level markdown parser

**Files:**
- Create: `platform/src/lib/curriculum.ts`
- Create: `platform/src/lib/curriculum.test.ts`
- Create: `platform/src/lib/types.ts`

- [ ] **Step 1: Write `types.ts`**

File: `platform/src/lib/types.ts`
```ts
export type Track = "A" | "B";
export type LevelStatus = "pending" | "in-progress" | "done";

export type QuizType = "self-attest" | "multiple-choice" | "free-text";

export interface QuizQuestion {
  q: string;
  type: QuizType;
  choices?: string[];
  answer?: number;
}

export interface LevelFrontmatter {
  id: string;
  title: string;
  track: Track;
  tier: number;
  xp: number;
  prereqs: string[];
  unlocks: string[];
  status: LevelStatus;
  completed_at: string | null;
  tags?: string[];
  achievements?: string[];
  luminary?: string | null;
  glossary_terms?: string[];
  recall_of?: string[];
  quiz?: QuizQuestion[];
}

export interface Level extends LevelFrontmatter {
  body: string;
  slug: string;
  sourcePath: string;
}

export interface CurriculumMeta {
  version: number;
  tiers: Array<{
    id: number;
    name: string;
    accent: string;
  }>;
  tier_bands: Array<{
    name: string;
    min_xp: number;
  }>;
}
```

- [ ] **Step 2: Write the failing test for `parseLevel`**

File: `platform/src/lib/curriculum.test.ts`
```ts
import { describe, it, expect } from "vitest";
import { parseLevel } from "./curriculum";

const sample = `---
id: F00
title: Welcome
track: A
tier: 0
xp: 1
prereqs: []
unlocks: [F01]
status: pending
completed_at: null
---

## Intro

Hello, **world**.
`;

describe("parseLevel", () => {
  it("parses frontmatter and body from a level markdown string", () => {
    const level = parseLevel(sample, "curriculum/levels/track-a/F00_welcome.md");
    expect(level.id).toBe("F00");
    expect(level.title).toBe("Welcome");
    expect(level.track).toBe("A");
    expect(level.tier).toBe(0);
    expect(level.xp).toBe(1);
    expect(level.prereqs).toEqual([]);
    expect(level.unlocks).toEqual(["F01"]);
    expect(level.body.trim().startsWith("## Intro")).toBe(true);
    expect(level.slug).toBe("F00_welcome");
  });

  it("throws on missing required frontmatter", () => {
    const bad = "---\ntitle: Missing ID\n---\n";
    expect(() => parseLevel(bad, "x.md")).toThrow(/missing.*id/i);
  });
});
```

- [ ] **Step 3: Run the test to confirm it fails**

```bash
pnpm test:run src/lib/curriculum.test.ts
```

Expected: fails with "Cannot find module './curriculum'".

- [ ] **Step 4: Implement `parseLevel` and `loadCurriculum`**

File: `platform/src/lib/curriculum.ts`
```ts
import matter from "gray-matter";
import type { Level, LevelFrontmatter } from "./types";

function basename(p: string): string {
  const m = p.match(/([^/\\]+)\.md$/);
  return m ? (m[1] as string) : p;
}

export function parseLevel(source: string, sourcePath: string): Level {
  const { data, content } = matter(source);
  const fm = data as Partial<LevelFrontmatter>;

  if (!fm.id) throw new Error(`Level at ${sourcePath} is missing required frontmatter: id`);
  if (!fm.title) throw new Error(`Level at ${sourcePath} is missing required frontmatter: title`);
  if (!fm.track) throw new Error(`Level at ${sourcePath} is missing required frontmatter: track`);
  if (fm.tier === undefined) throw new Error(`Level at ${sourcePath} is missing required frontmatter: tier`);
  if (fm.xp === undefined) throw new Error(`Level at ${sourcePath} is missing required frontmatter: xp`);

  const level: Level = {
    id: fm.id,
    title: fm.title,
    track: fm.track,
    tier: fm.tier,
    xp: fm.xp,
    prereqs: fm.prereqs ?? [],
    unlocks: fm.unlocks ?? [],
    status: fm.status ?? "pending",
    completed_at: fm.completed_at ?? null,
    tags: fm.tags,
    achievements: fm.achievements,
    luminary: fm.luminary ?? null,
    glossary_terms: fm.glossary_terms,
    recall_of: fm.recall_of,
    quiz: fm.quiz,
    body: content,
    slug: basename(sourcePath),
    sourcePath,
  };
  return level;
}

export async function loadCurriculum(base: string = "curriculum"): Promise<Level[]> {
  /**
   * Runtime-fetch every level markdown file. Files are bundled at build time by
   * Vite via import.meta.glob; the generated loader returns their URLs + contents.
   */
  const modules = import.meta.glob("/curriculum/levels/**/*.md", {
    eager: true,
    query: "?raw",
    import: "default",
  }) as Record<string, string>;

  const levels: Level[] = [];
  for (const [path, source] of Object.entries(modules)) {
    if (!path.startsWith(`/${base}/`)) continue;
    levels.push(parseLevel(source, path));
  }
  return levels.sort((a, b) => a.id.localeCompare(b.id));
}
```

- [ ] **Step 5: Run the test to verify it passes**

```bash
pnpm test:run src/lib/curriculum.test.ts
```

Expected: both tests pass.

- [ ] **Step 6: Commit**

```bash
git add platform/src/lib/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): level markdown parser with strict frontmatter validation

parseLevel parses a single markdown file with gray-matter and validates
the required frontmatter fields. loadCurriculum uses Vite's
import.meta.glob to enumerate all level files under curriculum/ at build
time while keeping the source markdown as the authoritative form.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Sample levels (3 starter levels for testing)

**Files:**
- Create: `curriculum/README.md`
- Create: `curriculum/meta.json`
- Create: `curriculum/levels/track-a/F00_welcome.md`
- Create: `curriculum/levels/track-a/F01_dev_environment.md`
- Create: `curriculum/levels/track-a/F02_how_to_read_a_paper.md`
- Create: `curriculum/achievements/definitions.md`

- [ ] **Step 1: Write `curriculum/meta.json`**

File: `curriculum/meta.json`
```json
{
  "version": 1,
  "name": "Road to AI",
  "description": "A cold-start-friendly path from math foundations to world models.",
  "tiers": [
    { "id": 0, "name": "Prelude",            "accent": "slate" },
    { "id": 1, "name": "Math Foundations",   "accent": "indigo" },
    { "id": 2, "name": "Classical ML",       "accent": "emerald" },
    { "id": 3, "name": "Symbolic AI",        "accent": "amber" },
    { "id": 4, "name": "Deep Learning",      "accent": "violet" },
    { "id": 5, "name": "Reinforcement Learning", "accent": "rose" },
    { "id": 6, "name": "LLMs",               "accent": "cyan" },
    { "id": 7, "name": "Agentic AI",         "accent": "teal" },
    { "id": 8, "name": "World Models",       "accent": "fuchsia" }
  ],
  "tier_bands": [
    { "name": "Novice",       "min_xp": 0 },
    { "name": "Apprentice",   "min_xp": 100 },
    { "name": "Journeyman",   "min_xp": 400 },
    { "name": "Practitioner", "min_xp": 1000 },
    { "name": "Specialist",   "min_xp": 2000 },
    { "name": "Researcher",   "min_xp": 3500 }
  ]
}
```

- [ ] **Step 2: Write `curriculum/README.md`**

File: `curriculum/README.md`
```markdown
# Road to AI Curriculum

The flagship content for the Road to AI platform. Licensed **CC-BY-SA 4.0**.

## Level file format

Every level is a single markdown file under `levels/track-a/` (foundations) or `levels/track-b/` (frontier). Naming: `<ID>_<slug>.md`, e.g., `F14_gradient_descent.md`.

## Frontmatter

See the design spec for the full schema: `docs/superpowers/specs/2026-04-15-road-to-ai-curriculum-and-platform-design.md#6c--level-file-format`.

Required fields: `id`, `title`, `track`, `tier`, `xp`, `prereqs`, `unlocks`, `status`, `completed_at`.

## Authoring rules

1. Every level **must** include a "Why this level matters (lineage)" section naming at least one classical root and at least one modern descendant (or vice versa for Track B).
2. If `luminary` is set, the body must include a "Luminary spotlight" section of 3–5 sentences.
3. If `glossary_terms` are declared, each term must exist in `glossary.md`.
4. Aim for ≥1 visual element per level (Mermaid diagram, equation, image, or code block).

## Contributing

See root `CONTRIBUTING.md`.
```

- [ ] **Step 3: Write `curriculum/levels/track-a/F00_welcome.md`**

File: `curriculum/levels/track-a/F00_welcome.md`
```markdown
---
id: F00
title: Welcome to Road to AI
track: A
tier: 0
xp: 1
prereqs: []
unlocks: [F01]
status: pending
completed_at: null
tags: [meta]
---

## Why this level matters (lineage)

**Classical root:** deliberate practice — a concept formalized by K. Anders Ericsson in the 1990s but recognizable in apprenticeship traditions stretching back centuries.
**Modern descendant:** every serious self-directed learning program — fast.ai, CS231n study groups, the DeepLearning.AI specializations — treats structured, staged, hands-on practice as non-negotiable. Road to AI is just that, gamified.

## Objectives

- Understand the two-track structure (Foundations + Frontier).
- Know how XP, levels, and boss artifacts work.
- Commit to the mindset: no streaks, no shortcuts, no pretending to understand.

## Resources

- The spec: [`docs/superpowers/specs/2026-04-15-road-to-ai-curriculum-and-platform-design.md`](../../docs/superpowers/specs/2026-04-15-road-to-ai-curriculum-and-platform-design.md)

## Tasks

- [ ] Read the spec sections 1–3 and 6.
- [ ] Open the app, find this level on the skill tree, see it light up.
- [ ] Mark this level done.

## Done criteria

You can summarize the Shape 3 dual-track model in one sentence.

## Bridge to modern

Completing this unlocks **F01 — Dev Environment**, the practical setup you'll rely on for years.
```

- [ ] **Step 4: Write `curriculum/levels/track-a/F01_dev_environment.md`**

File: `curriculum/levels/track-a/F01_dev_environment.md`
```markdown
---
id: F01
title: Dev Environment — Python, uv, Jupyter, Git
track: A
tier: 0
xp: 3
prereqs: [F00]
unlocks: [F02]
status: pending
completed_at: null
tags: [setup]
---

## Why this level matters (lineage)

**Classical root:** the REPL, born in Lisp (McCarthy, late 1950s) — an interactive loop for thinking in code.
**Modern descendant:** Jupyter notebooks, VS Code interactive Python, Colab, and Cursor — all direct descendants of the REPL idea. The workflow you set up here you'll use for every single subsequent level.

## Objectives

- Install a modern Python toolchain that you can reuse without version fights.
- Run a notebook locally and commit it to git.
- Confirm you can import numpy, matplotlib, and torch without errors.

## Resources

- [uv documentation](https://docs.astral.sh/uv/) — the fast Python package manager from Astral
- [Jupyter Lab quickstart](https://jupyterlab.readthedocs.io/en/stable/getting_started/starting.html)

## Tasks

- [ ] Install `uv`: `curl -LsSf https://astral.sh/uv/install.sh | sh` (macOS/Linux) or `irm https://astral.sh/uv/install.ps1 | iex` (Windows PowerShell).
- [ ] Create a project: `uv init rtai-scratch && cd rtai-scratch && uv add numpy matplotlib torch jupyterlab`
- [ ] Run `uv run jupyter lab` and open a notebook.
- [ ] In a cell, run `import torch; print(torch.__version__)`. If it prints a version, you're done.
- [ ] `git init`, commit the notebook.

## Done criteria

You can create a new Python project with one command, import torch in a notebook, and commit the result.

## Bridge to modern

Unlocks **F02 — How to Read a Paper**, which will rely on this environment for every reproduction exercise.
```

- [ ] **Step 5: Write `curriculum/levels/track-a/F02_how_to_read_a_paper.md`**

File: `curriculum/levels/track-a/F02_how_to_read_a_paper.md`
```markdown
---
id: F02
title: How to Read a Paper — Keshav's Three-Pass Method
track: A
tier: 0
xp: 2
prereqs: [F01]
unlocks: [F03]
status: pending
completed_at: null
tags: [meta, papers]
luminary: "S. Keshav"
---

## Why this level matters (lineage)

**Classical root:** close reading — the humanities tradition of reading the same passage three times at different depths.
**Modern descendant:** Keshav's 2007 paper "How to Read a Paper" adapted this for CS. Every DL researcher alive has read one or the other.

## Objectives

- Learn the three passes: skim, content, critical.
- Apply all three to a real AI paper.

## Resources

- [Keshav, "How to Read a Paper" (2 pages, PDF)](https://web.stanford.edu/class/ee384m/Handouts/HowtoReadPaper.pdf)
- Paper to practice on: Karpathy's "Software 2.0" Medium post (short, opinionated, foundational).

## Tasks

- [ ] Read Keshav's paper. Takes ~10 minutes.
- [ ] Apply the three-pass method to Karpathy's "Software 2.0". Spend no more than 45 minutes total.
- [ ] Write a one-paragraph summary in `notes/F02.md` capturing: what's the paper's main claim, what evidence, what would you critique.

## Done criteria

You can explain the three passes from memory and have produced a short written summary of "Software 2.0".

## Luminary spotlight — S. Keshav (b. 1965)

Professor of Computer Science at the University of Cambridge (previously Waterloo), known for networking research (TCP/IP, content-centric networking) but cited ten times more often for a one-page CV aside: his 2007 note "How to Read a Paper". He wrote it to help his own students and ended up providing the default mental model for a generation of engineers learning to navigate the research literature. The method works well precisely because it sets expectations — a first pass *should* feel shallow.

## Bridge to modern

You'll now be able to attempt your first Track B level (P01) without drowning.
```

- [ ] **Step 6: Write `curriculum/achievements/definitions.md`**

File: `curriculum/achievements/definitions.md`
```markdown
# Achievement Definitions

Each entry: id (kebab-case, platform-facing), title (human-facing), trigger (when it fires), level (rarity tier).

## first-step
- **Title:** First Step
- **Trigger:** Complete any level
- **Level:** common

## met-10-luminaries
- **Title:** Met 10 Luminaries
- **Trigger:** Encountered 10 distinct luminary spotlights
- **Level:** uncommon

## turing-award-trio
- **Title:** Met the Turing Trio
- **Trigger:** Encountered luminary spotlights for Hinton, LeCun, AND Bengio
- **Level:** rare

## backprop-by-hand
- **Title:** Derived Backprop by Hand
- **Trigger:** Completed F25 (Math Boss)
- **Level:** rare

## first-paper-reproduced
- **Title:** First Paper Reproduced
- **Trigger:** Completed any Track B level at P36+ (Raw tier)
- **Level:** rare

## glossary-trailblazer
- **Title:** Glossary Trailblazer
- **Trigger:** Encountered 25 distinct glossary terms via chips
- **Level:** uncommon

## recall-champion
- **Title:** Recall Champion
- **Trigger:** Completed 10 Recall Challenges
- **Level:** uncommon
```

- [ ] **Step 7: Verify levels load via the parser**

Add a quick sanity test. File: `platform/src/lib/curriculum.integration.test.ts`
```ts
import { describe, it, expect } from "vitest";
import { loadCurriculum } from "./curriculum";

describe("loadCurriculum", () => {
  it("loads all three starter levels", async () => {
    const levels = await loadCurriculum();
    expect(levels.length).toBeGreaterThanOrEqual(3);
    const ids = levels.map((l) => l.id);
    expect(ids).toContain("F00");
    expect(ids).toContain("F01");
    expect(ids).toContain("F02");
  });

  it("orders levels by id", async () => {
    const levels = await loadCurriculum();
    const ids = levels.map((l) => l.id);
    const sorted = [...ids].sort();
    expect(ids).toEqual(sorted);
  });
});
```

Vite's `import.meta.glob` resolves paths relative to the project root. Make sure `platform/vite.config.ts` sets the root to the repo root (or use an absolute glob). Add to `vite.config.ts`:

```ts
export default defineConfig({
  // …existing config…
  plugins: [react(), tailwindcss()],
  base: "/road-to-ai/",
  root: path.resolve(__dirname),
  publicDir: path.resolve(__dirname, "../curriculum"),
  // the publicDir makes curriculum/ files available at runtime as /curriculum/…
  // …
});
```

Actually Vite's `publicDir` copies contents; use `assetsInclude` + `?raw` glob instead. Simpler: keep levels accessible via glob against `../curriculum/**/*.md`. Replace the glob pattern in `curriculum.ts`:

```ts
const modules = import.meta.glob("../../../curriculum/levels/**/*.md", {
  eager: true,
  query: "?raw",
  import: "default",
}) as Record<string, string>;
```

Run:
```bash
pnpm test:run
```

Expected: all tests pass (Divider + parseLevel + loadCurriculum).

- [ ] **Step 8: Commit**

```bash
cd "D:/Personal/Assistant/Learn"
git add curriculum/ platform/src/lib/curriculum.ts platform/src/lib/curriculum.integration.test.ts platform/vite.config.ts
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(curriculum): add meta, three starter levels, achievements

curriculum/meta.json defines the nine tiers and six tier-bands.
Three Tier-0 Prelude levels (F00 welcome, F01 dev env, F02 how to
read a paper) exercise the level parser end-to-end. F02 includes the
first luminary spotlight (S. Keshav). achievements/definitions.md
lists the starter set of 7 achievements.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Graph builder

**Files:**
- Create: `platform/src/lib/graph.ts`
- Create: `platform/src/lib/graph.test.ts`

- [ ] **Step 1: Write the failing test**

File: `platform/src/lib/graph.test.ts`
```ts
import { describe, it, expect } from "vitest";
import { buildGraphElements } from "./graph";
import type { Level } from "./types";

const mkLevel = (overrides: Partial<Level>): Level => ({
  id: "F00",
  title: "T",
  track: "A",
  tier: 0,
  xp: 1,
  prereqs: [],
  unlocks: [],
  status: "pending",
  completed_at: null,
  body: "",
  slug: "f00",
  sourcePath: "",
  ...overrides,
});

describe("buildGraphElements", () => {
  it("creates one node per level", () => {
    const levels = [
      mkLevel({ id: "F00", unlocks: ["F01"] }),
      mkLevel({ id: "F01", prereqs: ["F00"] }),
    ];
    const els = buildGraphElements(levels);
    const nodes = els.filter((e) => e.group === "nodes");
    expect(nodes).toHaveLength(2);
    expect(nodes.map((n) => n.data.id).sort()).toEqual(["F00", "F01"]);
  });

  it("creates an edge for each prereq relationship", () => {
    const levels = [
      mkLevel({ id: "F00" }),
      mkLevel({ id: "F01", prereqs: ["F00"] }),
    ];
    const els = buildGraphElements(levels);
    const edges = els.filter((e) => e.group === "edges");
    expect(edges).toHaveLength(1);
    expect(edges[0].data.source).toBe("F00");
    expect(edges[0].data.target).toBe("F01");
  });

  it("marks unlocked levels whose prereqs are done", () => {
    const levels = [
      mkLevel({ id: "F00", status: "done" }),
      mkLevel({ id: "F01", prereqs: ["F00"], status: "pending" }),
      mkLevel({ id: "F02", prereqs: ["F00", "F01"], status: "pending" }),
    ];
    const els = buildGraphElements(levels);
    const byId = Object.fromEntries(
      els.filter((e) => e.group === "nodes").map((n) => [n.data.id, n.data]),
    );
    expect(byId["F01"].unlocked).toBe(true);
    expect(byId["F02"].unlocked).toBe(false);
  });
});
```

- [ ] **Step 2: Run the test to confirm it fails**

```bash
pnpm test:run src/lib/graph.test.ts
```

Expected: fails with "Cannot find module './graph'".

- [ ] **Step 3: Implement `buildGraphElements`**

File: `platform/src/lib/graph.ts`
```ts
import type { Level } from "./types";

export interface GraphNode {
  group: "nodes";
  data: {
    id: string;
    label: string;
    track: "A" | "B";
    tier: number;
    status: "pending" | "in-progress" | "done";
    unlocked: boolean;
    xp: number;
  };
}

export interface GraphEdge {
  group: "edges";
  data: {
    id: string;
    source: string;
    target: string;
  };
}

export type GraphElement = GraphNode | GraphEdge;

export function buildGraphElements(levels: Level[]): GraphElement[] {
  const byId = new Map(levels.map((l) => [l.id, l]));
  const elements: GraphElement[] = [];

  for (const level of levels) {
    const allPrereqsDone = level.prereqs.every((p) => byId.get(p)?.status === "done");
    const unlocked = level.prereqs.length === 0 || allPrereqsDone;

    elements.push({
      group: "nodes",
      data: {
        id: level.id,
        label: level.title,
        track: level.track,
        tier: level.tier,
        status: level.status,
        unlocked,
        xp: level.xp,
      },
    });

    for (const prereq of level.prereqs) {
      if (!byId.has(prereq)) continue;
      elements.push({
        group: "edges",
        data: {
          id: `${prereq}->${level.id}`,
          source: prereq,
          target: level.id,
        },
      });
    }
  }

  return elements;
}
```

- [ ] **Step 4: Run the test to verify it passes**

```bash
pnpm test:run src/lib/graph.test.ts
```

Expected: all 3 tests pass.

- [ ] **Step 5: Commit**

```bash
git add platform/src/lib/graph.ts platform/src/lib/graph.test.ts
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): graph builder turns levels into Cytoscape elements

buildGraphElements computes per-node unlock state from prereq
completion and emits Cytoscape-compatible node+edge records.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: SkillTree component

**Files:**
- Create: `platform/src/components/SkillTree.tsx`
- Create: `platform/src/components/SkillTree.test.tsx`

- [ ] **Step 1: Write the failing test**

File: `platform/src/components/SkillTree.test.tsx`
```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect, vi } from "vitest";
import { SkillTree } from "./SkillTree";
import type { Level } from "@/lib/types";

vi.mock("cytoscape", () => ({
  default: vi.fn(() => ({
    on: vi.fn(),
    destroy: vi.fn(),
    layout: vi.fn(() => ({ run: vi.fn() })),
  })),
}));

const mkLevel = (o: Partial<Level>): Level => ({
  id: "F00", title: "T", track: "A", tier: 0, xp: 1, prereqs: [], unlocks: [],
  status: "pending", completed_at: null, body: "", slug: "", sourcePath: "", ...o,
});

describe("SkillTree", () => {
  it("renders a container element", () => {
    render(<SkillTree levels={[mkLevel({})]} onSelect={() => {}} />);
    expect(screen.getByTestId("skill-tree")).toBeInTheDocument();
  });

  it("renders a list-view fallback for accessibility", () => {
    render(
      <SkillTree levels={[mkLevel({ id: "F00", title: "Welcome" })]} onSelect={() => {}} />,
    );
    expect(screen.getByRole("list", { name: /skill tree list view/i })).toBeInTheDocument();
    expect(screen.getByRole("listitem")).toHaveTextContent("Welcome");
  });
});
```

- [ ] **Step 2: Run the test to confirm it fails**

```bash
pnpm test:run src/components/SkillTree.test.tsx
```

Expected: "Cannot find module './SkillTree'".

- [ ] **Step 3: Implement `SkillTree`**

File: `platform/src/components/SkillTree.tsx`
```tsx
import cytoscape from "cytoscape";
import type { Core } from "cytoscape";
import { useEffect, useRef } from "react";
import type { Level } from "@/lib/types";
import { buildGraphElements } from "@/lib/graph";

export interface SkillTreeProps {
  levels: Level[];
  onSelect: (levelId: string) => void;
}

const STYLE: cytoscape.Stylesheet[] = [
  {
    selector: "node",
    style: {
      "background-color": "var(--color-surface)",
      "border-width": 1,
      "border-color": "var(--color-border)",
      label: "data(label)",
      "font-family": "var(--font-mono)",
      "font-size": 10,
      color: "var(--color-fg)",
      "text-valign": "bottom",
      "text-margin-y": 6,
      width: 32,
      height: 32,
    },
  },
  {
    selector: 'node[status = "done"]',
    style: { "background-color": "var(--color-accent)", "border-color": "var(--color-accent)" },
  },
  {
    selector: 'node[status = "in-progress"]',
    style: { "border-color": "var(--color-accent)", "border-width": 3 },
  },
  {
    selector: 'node[unlocked = "false"]',
    style: { opacity: 0.35 },
  },
  {
    selector: "edge",
    style: {
      width: 1,
      "line-color": "var(--color-border)",
      "target-arrow-color": "var(--color-border)",
      "target-arrow-shape": "triangle",
      "curve-style": "bezier",
    },
  },
];

export function SkillTree({ levels, onSelect }: SkillTreeProps) {
  const containerRef = useRef<HTMLDivElement>(null);
  const cyRef = useRef<Core | null>(null);

  useEffect(() => {
    if (!containerRef.current) return;
    const elements = buildGraphElements(levels).map((e) => ({
      ...e,
      data: { ...e.data, unlocked: String(("unlocked" in e.data ? e.data.unlocked : false)) },
    }));
    cyRef.current = cytoscape({
      container: containerRef.current,
      elements,
      style: STYLE,
      layout: { name: "breadthfirst", directed: true, padding: 24, spacingFactor: 1.2 },
      wheelSensitivity: 0.2,
    });
    cyRef.current.on("tap", "node", (evt) => {
      const id = evt.target.data("id");
      if (id) onSelect(id);
    });
    return () => {
      cyRef.current?.destroy();
      cyRef.current = null;
    };
  }, [levels, onSelect]);

  return (
    <>
      <div
        ref={containerRef}
        data-testid="skill-tree"
        className="h-[70vh] w-full rounded border border-[var(--color-border)]"
        role="img"
        aria-label="Skill tree graph"
      />
      <ul
        className="sr-only"
        aria-label="Skill tree list view"
      >
        {levels.map((l) => (
          <li key={l.id}>
            <button onClick={() => onSelect(l.id)}>
              {l.id} — {l.title} ({l.status})
            </button>
          </li>
        ))}
      </ul>
    </>
  );
}
```

- [ ] **Step 4: Run the test to verify it passes**

```bash
pnpm test:run src/components/SkillTree.test.tsx
```

Expected: both tests pass.

- [ ] **Step 5: Commit**

```bash
git add platform/src/components/SkillTree.tsx platform/src/components/SkillTree.test.tsx
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): SkillTree Cytoscape wrapper with a11y list fallback

Renders a breadthfirst-laid-out graph using CSS custom properties so
styling tracks the theme automatically. Locked nodes dim; in-progress
get an accent border; done nodes fill with the accent color. A
screen-reader-only <ul> mirrors the graph for keyboard users.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: LevelPane component with markdown rendering

**Files:**
- Create: `platform/src/lib/markdown.ts`
- Create: `platform/src/lib/markdown.test.ts`
- Create: `platform/src/components/LevelPane.tsx`
- Create: `platform/src/components/LevelPane.test.tsx`

- [ ] **Step 1: Write failing test for `renderMarkdown`**

File: `platform/src/lib/markdown.test.ts`
```ts
import { describe, it, expect } from "vitest";
import { renderMarkdown } from "./markdown";

describe("renderMarkdown", () => {
  it("renders headings and paragraphs as HTML", async () => {
    const html = await renderMarkdown("# Hello\n\nWorld.");
    expect(html).toContain("<h1>Hello</h1>");
    expect(html).toContain("<p>World.</p>");
  });

  it("renders inline math with KaTeX", async () => {
    const html = await renderMarkdown("The equation $E=mc^2$ is famous.");
    expect(html).toMatch(/katex/);
  });

  it("passes through fenced code unchanged for Shiki later", async () => {
    const html = await renderMarkdown("```python\nprint('hi')\n```");
    expect(html).toMatch(/<pre><code class="language-python">/);
  });
});
```

- [ ] **Step 2: Run the test to confirm it fails**

```bash
pnpm test:run src/lib/markdown.test.ts
```

Expected: "Cannot find module './markdown'".

- [ ] **Step 3: Implement `renderMarkdown`**

File: `platform/src/lib/markdown.ts`
```ts
import { unified } from "unified";
import remarkParse from "remark-parse";
import remarkGfm from "remark-gfm";
import remarkMath from "remark-math";
import remarkRehype from "remark-rehype";
import rehypeKatex from "rehype-katex";
import rehypeStringify from "rehype-stringify";

export async function renderMarkdown(source: string): Promise<string> {
  const file = await unified()
    .use(remarkParse)
    .use(remarkGfm)
    .use(remarkMath)
    .use(remarkRehype, { allowDangerousHtml: false })
    .use(rehypeKatex)
    .use(rehypeStringify)
    .process(source);
  return String(file);
}
```

Add to `package.json` dependencies if missing: `remark-rehype`. Run `pnpm add remark-rehype`.

- [ ] **Step 4: Run the test to verify it passes**

```bash
pnpm test:run src/lib/markdown.test.ts
```

Expected: all 3 tests pass.

- [ ] **Step 5: Write failing test for `LevelPane`**

File: `platform/src/components/LevelPane.test.tsx`
```tsx
import { render, screen, waitFor } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { LevelPane } from "./LevelPane";
import type { Level } from "@/lib/types";

const level: Level = {
  id: "F00", title: "Welcome", track: "A", tier: 0, xp: 1,
  prereqs: [], unlocks: [], status: "pending", completed_at: null,
  body: "## Objectives\n\n- First objective\n\n$E = mc^2$",
  slug: "f00", sourcePath: "",
};

describe("LevelPane", () => {
  it("renders the level title and id", () => {
    render(<LevelPane level={level} onMarkDone={() => {}} />);
    expect(screen.getByRole("heading", { name: /Welcome/ })).toBeInTheDocument();
    expect(screen.getByText("F00")).toBeInTheDocument();
  });

  it("renders the body markdown", async () => {
    render(<LevelPane level={level} onMarkDone={() => {}} />);
    await waitFor(() => expect(screen.getByText("Objectives")).toBeInTheDocument());
    expect(screen.getByText(/First objective/)).toBeInTheDocument();
  });

  it("shows a Mark Done button when status is pending", () => {
    render(<LevelPane level={level} onMarkDone={() => {}} />);
    expect(screen.getByRole("button", { name: /mark done/i })).toBeInTheDocument();
  });
});
```

- [ ] **Step 6: Run the test to confirm it fails**

```bash
pnpm test:run src/components/LevelPane.test.tsx
```

Expected: "Cannot find module './LevelPane'".

- [ ] **Step 7: Implement `LevelPane`**

File: `platform/src/components/LevelPane.tsx`
```tsx
import { useEffect, useState } from "react";
import type { Level } from "@/lib/types";
import { renderMarkdown } from "@/lib/markdown";
import { Divider } from "./Divider";

export interface LevelPaneProps {
  level: Level;
  onMarkDone: (id: string) => void;
}

export function LevelPane({ level, onMarkDone }: LevelPaneProps) {
  const [html, setHtml] = useState<string>("");

  useEffect(() => {
    let cancelled = false;
    renderMarkdown(level.body).then((h) => {
      if (!cancelled) setHtml(h);
    });
    return () => { cancelled = true; };
  }, [level.body]);

  return (
    <article className="mx-auto max-w-prose px-6 py-10">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">
        {`// ${level.track === "A" ? "foundations" : "frontier"} · tier ${level.tier}`}
      </p>
      <div className="mt-2 flex items-baseline gap-3">
        <span className="font-mono text-sm text-[var(--color-muted)]">{level.id}</span>
        <h1 className="text-3xl font-semibold tracking-tight">{level.title}</h1>
      </div>
      <p className="mt-2 font-mono text-xs text-[var(--color-muted)]">
        {level.xp} XP · {level.status}
      </p>

      <Divider>content</Divider>
      <div
        className="prose prose-invert max-w-none prose-headings:font-semibold"
        dangerouslySetInnerHTML={{ __html: html }}
      />

      {level.status !== "done" && (
        <>
          <Divider>action</Divider>
          <button
            onClick={() => onMarkDone(level.id)}
            className="rounded border border-[var(--color-accent)] bg-[var(--color-accent)] px-4 py-2 font-mono text-sm text-[var(--color-accent-fg)] transition hover:opacity-90"
          >
            Mark done · +{level.xp} XP
          </button>
        </>
      )}
    </article>
  );
}
```

Add `@tailwindcss/typography` plugin: `pnpm add -D @tailwindcss/typography`. Then in `tailwind.config.ts`:

```ts
import typography from "@tailwindcss/typography";
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  darkMode: "class",
  theme: { extend: {} },
  plugins: [typography],
};
```

- [ ] **Step 8: Run all tests to verify**

```bash
pnpm test:run
```

Expected: every test suite passes (Divider, parseLevel, loadCurriculum, graph, SkillTree, renderMarkdown, LevelPane).

- [ ] **Step 9: Commit**

```bash
git add platform/src/lib/markdown.ts platform/src/lib/markdown.test.ts platform/src/components/LevelPane.tsx platform/src/components/LevelPane.test.tsx platform/package.json platform/tailwind.config.ts
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): LevelPane renders level markdown with math via KaTeX

renderMarkdown() pipes unified + remark + KaTeX to turn level body
markdown into HTML. LevelPane displays the level's id/title/xp/status
header with code-comment dividers and a Mark Done action that's
hidden once the level is already done.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: Mermaid + Shiki integration

**Files:**
- Modify: `platform/src/lib/markdown.ts` (add Mermaid + Shiki post-processing)
- Create: `platform/src/lib/mermaid.ts`
- Create: `platform/src/lib/shiki.ts`
- Create: `platform/src/styles/katex.css` (import KaTeX styles)
- Modify: `platform/src/styles/globals.css`

- [ ] **Step 1: Write the failing test for code block highlighting**

Add to `platform/src/lib/markdown.test.ts`:
```ts
it("highlights code blocks with Shiki after rendering", async () => {
  const html = await renderMarkdown("```python\nprint('hi')\n```");
  // Shiki-highlighted output wraps tokens in <span style="color:…"> within <pre class="shiki ...">
  expect(html).toMatch(/<pre class="shiki/);
  expect(html).toMatch(/<span style="color:/);
});

it("replaces mermaid fenced blocks with rendered SVG", async () => {
  const html = await renderMarkdown("```mermaid\ngraph TD; A-->B\n```");
  expect(html).toMatch(/<svg[^>]+aria-label=/);
});
```

- [ ] **Step 2: Run the test to see it fail**

```bash
pnpm test:run src/lib/markdown.test.ts
```

Expected: two new tests fail.

- [ ] **Step 3: Implement Shiki post-processing**

File: `platform/src/lib/shiki.ts`
```ts
import { getHighlighter, type Highlighter } from "shiki";

let highlighterPromise: Promise<Highlighter> | null = null;

export function getShikiHighlighter(): Promise<Highlighter> {
  if (!highlighterPromise) {
    highlighterPromise = getHighlighter({
      themes: ["github-dark", "github-light"],
      langs: ["python", "typescript", "javascript", "bash", "yaml", "json", "tsx", "jsx", "html", "css"],
    });
  }
  return highlighterPromise;
}

/**
 * Replace <pre><code class="language-xxx">…</code></pre> blocks produced by
 * remark-rehype with Shiki-highlighted HTML.
 */
export async function highlightHtml(html: string): Promise<string> {
  const highlighter = await getShikiHighlighter();
  return html.replace(
    /<pre><code class="language-([\w-]+)">([\s\S]*?)<\/code><\/pre>/g,
    (_full, lang: string, raw: string) => {
      const code = raw
        .replace(/&lt;/g, "<")
        .replace(/&gt;/g, ">")
        .replace(/&amp;/g, "&")
        .replace(/&quot;/g, '"');
      try {
        return highlighter.codeToHtml(code, { lang, themes: { dark: "github-dark", light: "github-light" } });
      } catch {
        return `<pre><code class="language-${lang}">${raw}</code></pre>`;
      }
    },
  );
}
```

- [ ] **Step 4: Implement Mermaid post-processing**

File: `platform/src/lib/mermaid.ts`
```ts
import mermaid from "mermaid";

let initialized = false;
function init() {
  if (initialized) return;
  mermaid.initialize({
    startOnLoad: false,
    theme: "dark",
    fontFamily: "JetBrains Mono, ui-monospace, monospace",
    securityLevel: "strict",
  });
  initialized = true;
}

/**
 * Replace mermaid fenced blocks with rendered SVG.
 */
export async function renderMermaid(html: string): Promise<string> {
  init();
  const matches = [...html.matchAll(/<pre><code class="language-mermaid">([\s\S]*?)<\/code><\/pre>/g)];
  let result = html;
  let i = 0;
  for (const match of matches) {
    const raw = match[1] as string;
    const code = raw
      .replace(/&lt;/g, "<")
      .replace(/&gt;/g, ">")
      .replace(/&amp;/g, "&")
      .replace(/&quot;/g, '"');
    try {
      const { svg } = await mermaid.render(`mermaid-${i++}-${Date.now()}`, code);
      result = result.replace(match[0], svg);
    } catch (err) {
      result = result.replace(
        match[0],
        `<pre class="text-xs text-red-500">Mermaid render error: ${(err as Error).message}</pre>`,
      );
    }
  }
  return result;
}
```

- [ ] **Step 5: Chain both into `renderMarkdown`**

Replace `platform/src/lib/markdown.ts` body with:
```ts
import { unified } from "unified";
import remarkParse from "remark-parse";
import remarkGfm from "remark-gfm";
import remarkMath from "remark-math";
import remarkRehype from "remark-rehype";
import rehypeKatex from "rehype-katex";
import rehypeStringify from "rehype-stringify";
import { highlightHtml } from "./shiki";
import { renderMermaid } from "./mermaid";

export async function renderMarkdown(source: string): Promise<string> {
  const file = await unified()
    .use(remarkParse)
    .use(remarkGfm)
    .use(remarkMath)
    .use(remarkRehype, { allowDangerousHtml: false })
    .use(rehypeKatex)
    .use(rehypeStringify)
    .process(source);
  let html = String(file);
  html = await renderMermaid(html);
  html = await highlightHtml(html);
  return html;
}
```

- [ ] **Step 6: Import KaTeX stylesheet**

Modify `platform/src/styles/globals.css` — add at top after tailwind import:
```css
@import "katex/dist/katex.min.css";
```

- [ ] **Step 7: Run all tests**

```bash
pnpm test:run
```

Expected: all tests pass including the two new markdown tests.

- [ ] **Step 8: Commit**

```bash
git add platform/src/lib/ platform/src/styles/globals.css
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): Mermaid diagrams and Shiki code highlighting in levels

Extends renderMarkdown to post-process rehype output: mermaid fenced
blocks become inline SVG and all language-tagged code blocks are
highlighted with Shiki's GitHub dark/light themes. KaTeX styles now
import from the package so equations render correctly without network.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 13: App shell and routing

**Files:**
- Modify: `platform/src/App.tsx`
- Create: `platform/src/pages/HomePage.tsx`
- Create: `platform/src/pages/LevelPage.tsx`
- Create: `platform/src/store/useStore.ts`

- [ ] **Step 1: Write the Zustand store**

File: `platform/src/store/useStore.ts`
```ts
import { create } from "zustand";
import type { Level } from "@/lib/types";
import { loadCurriculum } from "@/lib/curriculum";

interface StoreState {
  levels: Level[];
  loaded: boolean;
  selectedId: string | null;
  load: () => Promise<void>;
  select: (id: string | null) => void;
  markDone: (id: string) => void;
}

export const useStore = create<StoreState>((set, get) => ({
  levels: [],
  loaded: false,
  selectedId: null,
  async load() {
    if (get().loaded) return;
    const levels = await loadCurriculum();
    set({ levels, loaded: true });
  },
  select(id) {
    set({ selectedId: id });
  },
  markDone(id) {
    set((s) => ({
      levels: s.levels.map((l) =>
        l.id === id
          ? { ...l, status: "done", completed_at: new Date().toISOString().slice(0, 10) }
          : l,
      ),
    }));
    // Progress persistence is wired in Task 15.
  },
}));
```

- [ ] **Step 2: Write `HomePage`**

File: `platform/src/pages/HomePage.tsx`
```tsx
import { useEffect } from "react";
import { useStore } from "@/store/useStore";
import { SkillTree } from "@/components/SkillTree";
import { Divider } from "@/components/Divider";

export function HomePage() {
  const levels = useStore((s) => s.levels);
  const load = useStore((s) => s.load);
  const select = useStore((s) => s.select);

  useEffect(() => { void load(); }, [load]);

  return (
    <main className="mx-auto max-w-6xl px-6 py-10">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">// road-to-ai</p>
      <h1 className="mt-2 text-4xl font-semibold tracking-tight">Road to AI</h1>
      <p className="mt-4 text-[var(--color-muted)]">
        A gamified, open-source path from math to world models. Click any unlocked node to begin.
      </p>
      <Divider>skill tree</Divider>
      <SkillTree levels={levels} onSelect={select} />
    </main>
  );
}
```

- [ ] **Step 3: Write `LevelPage`**

File: `platform/src/pages/LevelPage.tsx`
```tsx
import { useStore } from "@/store/useStore";
import { LevelPane } from "@/components/LevelPane";

export function LevelPage() {
  const selectedId = useStore((s) => s.selectedId);
  const levels = useStore((s) => s.levels);
  const select = useStore((s) => s.select);
  const markDone = useStore((s) => s.markDone);

  const level = levels.find((l) => l.id === selectedId);
  if (!level) return null;

  return (
    <div className="fixed inset-0 z-10 flex bg-[var(--color-bg)]/95 backdrop-blur">
      <div className="mx-auto my-8 w-full max-w-3xl overflow-auto rounded-lg border border-[var(--color-border)] bg-[var(--color-bg)]">
        <div className="flex justify-end p-4">
          <button
            onClick={() => select(null)}
            className="font-mono text-xs text-[var(--color-muted)] hover:text-[var(--color-fg)]"
            aria-label="Close level"
          >
            [close]
          </button>
        </div>
        <LevelPane level={level} onMarkDone={markDone} />
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Wire `App.tsx`**

Replace `platform/src/App.tsx`:
```tsx
import { HomePage } from "./pages/HomePage";
import { LevelPage } from "./pages/LevelPage";
import { useStore } from "./store/useStore";

export default function App() {
  const selectedId = useStore((s) => s.selectedId);
  return (
    <>
      <HomePage />
      {selectedId && <LevelPage />}
    </>
  );
}
```

- [ ] **Step 5: Dev smoke test**

```bash
pnpm dev
```

Open `http://localhost:5173/road-to-ai/`:
- See the skill tree with F00, F01, F02 nodes; F00 unlocked, others dimmed.
- Click F00 — level pane opens, markdown renders (with the Luminary Spotlight for F02 when you open that one).
- Click "[close]" — returns to the tree.

Stop the server.

- [ ] **Step 6: Commit**

```bash
git add platform/src/App.tsx platform/src/pages/ platform/src/store/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): app shell, Zustand store, home + level pages

HomePage shows the skill tree; LevelPage opens a modal over it when a
level is selected. useStore manages curriculum loading, selection, and
the optimistic mark-done action (persistence comes in M3).

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 14: Commit M2 milestone and deploy

- [ ] **Step 1: Update CHANGELOG**

Append to `CHANGELOG.md`:
```markdown
## [platform-v0.1.0] — 2026-04-15

### Added
- Level markdown parser with strict frontmatter validation
- Cytoscape-powered skill tree with lock/unlock/in-progress/done states
- Level pane modal with Mermaid diagrams, KaTeX math, and Shiki code highlighting
- Zustand store orchestrating curriculum loading and selection
- First three Tier-0 levels (F00 welcome, F01 dev env, F02 paper reading)
- First luminary spotlight (S. Keshav in F02)
```

- [ ] **Step 2: Commit, tag, push**

```bash
git add CHANGELOG.md
git -c commit.gpgsign=false commit -m "docs: log platform-v0.1.0 M2 milestone"
git tag -a platform-v0.1.0 -m "M2: level loader + skill tree"
git push origin main
git push origin platform-v0.1.0
```

- [ ] **Step 3: Verify the live site**

```bash
gh run watch
```

Open `https://jvloo.github.io/road-to-ai/` — skill tree interactive on the live URL.

**M2 done.**

---

## M3 — Gamification core

**Outcome:** Complete a level in the UI, XP bar animates, achievements fire, the luminary index and glossary are browsable, text can be selected and translated.

### Task 15: Progress persistence (`progress.ts`)

**Files:**
- Create: `platform/src/lib/progress.ts`
- Create: `platform/src/lib/progress.test.ts`
- Modify: `platform/src/store/useStore.ts`
- Create: `progress.json` (root, gitignored)

- [ ] **Step 1: Write the failing test**

File: `platform/src/lib/progress.test.ts`
```ts
import { describe, it, expect, beforeEach } from "vitest";
import { loadProgress, saveProgress, markLevelDone } from "./progress";

beforeEach(() => { localStorage.clear(); });

describe("progress", () => {
  it("returns a default progress shape when none is saved", () => {
    const p = loadProgress();
    expect(p.version).toBe(1);
    expect(p.xp_total).toBe(0);
    expect(p.tier).toBe("Novice");
    expect(Object.keys(p.levels)).toHaveLength(0);
  });

  it("round-trips saved progress", () => {
    const p = loadProgress();
    p.xp_total = 50;
    p.levels["F00"] = { status: "done", completed_at: "2026-04-15" };
    saveProgress(p);
    const p2 = loadProgress();
    expect(p2.xp_total).toBe(50);
    expect(p2.levels["F00"]?.status).toBe("done");
  });

  it("markLevelDone updates status, timestamp, and xp", () => {
    const p = markLevelDone(loadProgress(), "F00", 3);
    expect(p.levels["F00"]?.status).toBe("done");
    expect(p.levels["F00"]?.completed_at).toMatch(/^\d{4}-\d{2}-\d{2}$/);
    expect(p.xp_total).toBe(3);
  });
});
```

- [ ] **Step 2: Run the test to confirm it fails**

```bash
pnpm test:run src/lib/progress.test.ts
```

Expected: "Cannot find module './progress'".

- [ ] **Step 3: Implement `progress.ts`**

File: `platform/src/lib/progress.ts`
```ts
export interface LevelProgress {
  status: "pending" | "in-progress" | "done";
  completed_at?: string;
  started_at?: string;
}

export interface AchievementEntry {
  id: string;
  earned_at: string;
}

export interface Progress {
  version: number;
  user: string;
  xp_total: number;
  tier: string;
  started_at: string | null;
  last_session_at: string | null;
  levels: Record<string, LevelProgress>;
  achievements: AchievementEntry[];
  stats: {
    sessions: number;
    hours_logged: number;
    levels_done: number;
  };
}

const STORAGE_KEY = "road-to-ai:progress:v1";

function defaultProgress(): Progress {
  return {
    version: 1,
    user: "local",
    xp_total: 0,
    tier: "Novice",
    started_at: null,
    last_session_at: null,
    levels: {},
    achievements: [],
    stats: { sessions: 0, hours_logged: 0, levels_done: 0 },
  };
}

export function loadProgress(): Progress {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return defaultProgress();
    const parsed = JSON.parse(raw) as Progress;
    return { ...defaultProgress(), ...parsed };
  } catch {
    return defaultProgress();
  }
}

export function saveProgress(p: Progress): void {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(p));
}

const TIERS: Array<{ name: string; min: number }> = [
  { name: "Novice", min: 0 },
  { name: "Apprentice", min: 100 },
  { name: "Journeyman", min: 400 },
  { name: "Practitioner", min: 1000 },
  { name: "Specialist", min: 2000 },
  { name: "Researcher", min: 3500 },
];

export function computeTier(xp: number): string {
  let tier = TIERS[0]!.name;
  for (const t of TIERS) if (xp >= t.min) tier = t.name;
  return tier;
}

export function markLevelDone(p: Progress, id: string, xp: number): Progress {
  const today = new Date().toISOString().slice(0, 10);
  const wasDone = p.levels[id]?.status === "done";
  const xpDelta = wasDone ? 0 : xp;
  const xp_total = p.xp_total + xpDelta;
  return {
    ...p,
    xp_total,
    tier: computeTier(xp_total),
    last_session_at: today,
    started_at: p.started_at ?? today,
    levels: { ...p.levels, [id]: { status: "done", completed_at: today } },
    stats: {
      ...p.stats,
      levels_done: p.stats.levels_done + (wasDone ? 0 : 1),
    },
  };
}
```

- [ ] **Step 4: Run the test to verify it passes**

```bash
pnpm test:run src/lib/progress.test.ts
```

Expected: 3 tests pass.

- [ ] **Step 5: Wire progress into the store**

Modify `platform/src/store/useStore.ts`:
```ts
import { create } from "zustand";
import type { Level } from "@/lib/types";
import { loadCurriculum } from "@/lib/curriculum";
import { loadProgress, markLevelDone, saveProgress, type Progress } from "@/lib/progress";

interface StoreState {
  levels: Level[];
  loaded: boolean;
  selectedId: string | null;
  progress: Progress;
  load: () => Promise<void>;
  select: (id: string | null) => void;
  markDone: (id: string) => void;
}

export const useStore = create<StoreState>((set, get) => ({
  levels: [],
  loaded: false,
  selectedId: null,
  progress: loadProgress(),
  async load() {
    if (get().loaded) return;
    const rawLevels = await loadCurriculum();
    const prog = get().progress;
    // Merge saved status over authored defaults
    const levels = rawLevels.map((l) => {
      const saved = prog.levels[l.id];
      return saved ? { ...l, status: saved.status, completed_at: saved.completed_at ?? null } : l;
    });
    set({ levels, loaded: true });
  },
  select(id) { set({ selectedId: id }); },
  markDone(id) {
    const s = get();
    const level = s.levels.find((l) => l.id === id);
    if (!level) return;
    const progress = markLevelDone(s.progress, id, level.xp);
    saveProgress(progress);
    const levels = s.levels.map((l) =>
      l.id === id ? { ...l, status: "done" as const, completed_at: new Date().toISOString().slice(0, 10) } : l,
    );
    set({ progress, levels });
  },
}));
```

- [ ] **Step 6: Run all tests**

```bash
pnpm test:run
```

Expected: everything passes.

- [ ] **Step 7: Commit**

```bash
git add platform/src/lib/progress.ts platform/src/lib/progress.test.ts platform/src/store/useStore.ts
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): persist progress to localStorage

loadProgress/saveProgress handle serialization; markLevelDone
increments xp, updates tier, stamps completion date. The Zustand
store merges saved progress over authored level defaults on load
and writes on every mark-done.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 16: XpBar and TierBadge

**Files:**
- Create: `platform/src/components/XpBar.tsx`
- Create: `platform/src/components/XpBar.test.tsx`
- Create: `platform/src/components/TierBadge.tsx`
- Create: `platform/src/components/TierBadge.test.tsx`

- [ ] **Step 1: XpBar failing test**

File: `platform/src/components/XpBar.test.tsx`
```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { XpBar } from "./XpBar";

describe("XpBar", () => {
  it("renders total xp and current tier bounds", () => {
    render(<XpBar xp={150} />);
    expect(screen.getByText(/150 XP/)).toBeInTheDocument();
    expect(screen.getByText(/Apprentice/)).toBeInTheDocument();
    expect(screen.getByText(/Journeyman/)).toBeInTheDocument();
  });

  it("at max tier shows only the researcher label", () => {
    render(<XpBar xp={4000} />);
    expect(screen.getByText(/Researcher/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run to confirm fail, then implement**

```bash
pnpm test:run src/components/XpBar.test.tsx
```

File: `platform/src/components/XpBar.tsx`
```tsx
const BANDS = [
  { name: "Novice", min: 0 },
  { name: "Apprentice", min: 100 },
  { name: "Journeyman", min: 400 },
  { name: "Practitioner", min: 1000 },
  { name: "Specialist", min: 2000 },
  { name: "Researcher", min: 3500 },
];

export function XpBar({ xp }: { xp: number }) {
  let currentIdx = 0;
  for (let i = 0; i < BANDS.length; i++) {
    if (xp >= (BANDS[i]?.min ?? 0)) currentIdx = i;
  }
  const current = BANDS[currentIdx]!;
  const next = BANDS[currentIdx + 1];
  const pct = next ? Math.min(100, ((xp - current.min) / (next.min - current.min)) * 100) : 100;

  return (
    <div className="w-full">
      <div className="flex justify-between font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">
        <span>{current.name}</span>
        <span>{xp} XP</span>
        {next && <span>{next.name}</span>}
      </div>
      <div className="mt-2 h-1 w-full rounded bg-[var(--color-border)] overflow-hidden">
        <div
          className="h-full bg-[var(--color-accent)] transition-[width] duration-500"
          style={{ width: `${pct}%` }}
        />
      </div>
    </div>
  );
}
```

Run:
```bash
pnpm test:run src/components/XpBar.test.tsx
```

Expected: 2 tests pass.

- [ ] **Step 3: TierBadge failing test**

File: `platform/src/components/TierBadge.test.tsx`
```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { TierBadge } from "./TierBadge";

describe("TierBadge", () => {
  it("renders the tier name in a monospace pill", () => {
    render(<TierBadge tier="Apprentice" />);
    const el = screen.getByText(/Apprentice/);
    expect(el).toBeInTheDocument();
  });
});
```

File: `platform/src/components/TierBadge.tsx`
```tsx
export function TierBadge({ tier }: { tier: string }) {
  return (
    <span className="inline-block rounded border border-[var(--color-border)] px-2 py-0.5 font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">
      {tier}
    </span>
  );
}
```

- [ ] **Step 4: Run all tests and commit**

```bash
pnpm test:run
git add platform/src/components/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): XpBar and TierBadge progression components

XpBar shows the current tier, next tier, and total XP with a
width-animated fill. TierBadge renders a simple monospace pill.
Both styled from CSS variables so theme swaps track automatically.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 17: Achievements engine

**Files:**
- Create: `platform/src/lib/achievements.ts`
- Create: `platform/src/lib/achievements.test.ts`
- Modify: `platform/src/store/useStore.ts`

- [ ] **Step 1: Write the failing test**

File: `platform/src/lib/achievements.test.ts`
```ts
import { describe, it, expect } from "vitest";
import { checkAchievements } from "./achievements";
import type { Progress } from "./progress";
import type { Level } from "./types";

const mkLevel = (o: Partial<Level>): Level => ({
  id: "F00", title: "", track: "A", tier: 0, xp: 1, prereqs: [], unlocks: [],
  status: "pending", completed_at: null, body: "", slug: "", sourcePath: "", ...o,
});

const mkProgress = (overrides: Partial<Progress> = {}): Progress => ({
  version: 1, user: "local", xp_total: 0, tier: "Novice", started_at: null, last_session_at: null,
  levels: {}, achievements: [], stats: { sessions: 0, hours_logged: 0, levels_done: 0 }, ...overrides,
});

describe("checkAchievements", () => {
  it("awards first-step on first level completion", () => {
    const p = mkProgress({ stats: { sessions: 0, hours_logged: 0, levels_done: 1 } });
    const newAwards = checkAchievements(p, [mkLevel({})]);
    expect(newAwards.map((a) => a.id)).toContain("first-step");
  });

  it("does not re-award previously earned achievements", () => {
    const p = mkProgress({
      stats: { sessions: 0, hours_logged: 0, levels_done: 1 },
      achievements: [{ id: "first-step", earned_at: "2026-04-15" }],
    });
    const newAwards = checkAchievements(p, [mkLevel({})]);
    expect(newAwards.map((a) => a.id)).not.toContain("first-step");
  });

  it("awards met-10-luminaries when 10 distinct luminaries have been encountered in done levels", () => {
    const levels = Array.from({ length: 10 }, (_, i) =>
      mkLevel({ id: `F${i}`, luminary: `Person ${i}`, status: "done" }),
    );
    const p = mkProgress({
      levels: Object.fromEntries(levels.map((l) => [l.id, { status: "done" as const }])),
    });
    const newAwards = checkAchievements(p, levels);
    expect(newAwards.map((a) => a.id)).toContain("met-10-luminaries");
  });
});
```

- [ ] **Step 2: Run test to confirm fail**

```bash
pnpm test:run src/lib/achievements.test.ts
```

- [ ] **Step 3: Implement**

File: `platform/src/lib/achievements.ts`
```ts
import type { Progress, AchievementEntry } from "./progress";
import type { Level } from "./types";

type Rule = (p: Progress, levels: Level[]) => boolean;

const RULES: Record<string, Rule> = {
  "first-step": (p) => p.stats.levels_done >= 1,
  "met-10-luminaries": (p, levels) => {
    const done = new Set(Object.entries(p.levels).filter(([_, v]) => v.status === "done").map(([k]) => k));
    const set = new Set<string>();
    for (const l of levels) {
      if (l.luminary && done.has(l.id)) set.add(l.luminary);
    }
    return set.size >= 10;
  },
  "met-25-luminaries": (p, levels) => {
    const done = new Set(Object.entries(p.levels).filter(([_, v]) => v.status === "done").map(([k]) => k));
    const set = new Set<string>();
    for (const l of levels) {
      if (l.luminary && done.has(l.id)) set.add(l.luminary);
    }
    return set.size >= 25;
  },
  "glossary-trailblazer": () => false, // wired in Task 19 when chip encounters are tracked
};

export function checkAchievements(p: Progress, levels: Level[]): AchievementEntry[] {
  const earnedIds = new Set(p.achievements.map((a) => a.id));
  const today = new Date().toISOString().slice(0, 10);
  const newAwards: AchievementEntry[] = [];
  for (const [id, rule] of Object.entries(RULES)) {
    if (!earnedIds.has(id) && rule(p, levels)) {
      newAwards.push({ id, earned_at: today });
    }
  }
  return newAwards;
}
```

- [ ] **Step 4: Wire into store's `markDone`**

Modify `useStore.ts`'s `markDone`:
```ts
import { checkAchievements } from "@/lib/achievements";

// inside markDone:
markDone(id) {
  const s = get();
  const level = s.levels.find((l) => l.id === id);
  if (!level) return;
  let progress = markLevelDone(s.progress, id, level.xp);
  const levels = s.levels.map((l) =>
    l.id === id ? { ...l, status: "done" as const, completed_at: new Date().toISOString().slice(0, 10) } : l,
  );
  const newAwards = checkAchievements(progress, levels);
  if (newAwards.length) {
    progress = { ...progress, achievements: [...progress.achievements, ...newAwards] };
  }
  saveProgress(progress);
  set({ progress, levels });
},
```

- [ ] **Step 5: Run tests and commit**

```bash
pnpm test:run
git add platform/src/lib/achievements.ts platform/src/lib/achievements.test.ts platform/src/store/useStore.ts
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): achievements engine + store integration

RULES map predicate to achievement id. checkAchievements runs all
unfired rules on each mark-done and returns new awards. Two
luminary-breadth rules seeded; glossary-trailblazer rule will be
wired when chip encounters are tracked in Task 19.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 18: AchievementShelf + ProgressDashboard

**Files:**
- Create: `platform/src/components/AchievementShelf.tsx`
- Create: `platform/src/components/ProgressDashboard.tsx`
- Modify: `platform/src/pages/HomePage.tsx`

- [ ] **Step 1: AchievementShelf**

File: `platform/src/components/AchievementShelf.tsx`
```tsx
import type { AchievementEntry } from "@/lib/progress";

const TITLES: Record<string, string> = {
  "first-step": "First Step",
  "met-10-luminaries": "Met 10 Luminaries",
  "met-25-luminaries": "Met 25 Luminaries",
  "turing-award-trio": "Met the Turing Trio",
  "backprop-by-hand": "Derived Backprop by Hand",
  "first-paper-reproduced": "First Paper Reproduced",
  "glossary-trailblazer": "Glossary Trailblazer",
  "recall-champion": "Recall Champion",
};

export function AchievementShelf({ achievements }: { achievements: AchievementEntry[] }) {
  if (!achievements.length) {
    return (
      <p className="font-mono text-xs text-[var(--color-muted)]">
        // no achievements yet — complete a level
      </p>
    );
  }
  return (
    <ul className="flex flex-wrap gap-2">
      {achievements.map((a) => (
        <li
          key={a.id}
          className="rounded border border-[var(--color-accent)] px-2 py-1 font-mono text-xs text-[var(--color-accent)]"
          title={`Earned ${a.earned_at}`}
        >
          ◆ {TITLES[a.id] ?? a.id}
        </li>
      ))}
    </ul>
  );
}
```

- [ ] **Step 2: ProgressDashboard**

File: `platform/src/components/ProgressDashboard.tsx`
```tsx
import { XpBar } from "./XpBar";
import { AchievementShelf } from "./AchievementShelf";
import { Divider } from "./Divider";
import type { Progress } from "@/lib/progress";

export function ProgressDashboard({ progress }: { progress: Progress }) {
  return (
    <section>
      <XpBar xp={progress.xp_total} />
      <Divider>achievements</Divider>
      <AchievementShelf achievements={progress.achievements} />
      <Divider>stats</Divider>
      <dl className="grid grid-cols-3 gap-4 font-mono text-xs">
        <div>
          <dt className="text-[var(--color-muted)]">levels done</dt>
          <dd className="text-2xl">{progress.stats.levels_done}</dd>
        </div>
        <div>
          <dt className="text-[var(--color-muted)]">current tier</dt>
          <dd className="text-2xl">{progress.tier}</dd>
        </div>
        <div>
          <dt className="text-[var(--color-muted)]">last session</dt>
          <dd className="text-sm">{progress.last_session_at ?? "—"}</dd>
        </div>
      </dl>
    </section>
  );
}
```

- [ ] **Step 3: Add ProgressDashboard to HomePage**

Modify `platform/src/pages/HomePage.tsx` — after the `<h1>` and intro paragraph, insert a `<ProgressDashboard>` and a divider before the skill tree:

```tsx
import { ProgressDashboard } from "@/components/ProgressDashboard";

export function HomePage() {
  const levels = useStore((s) => s.levels);
  const progress = useStore((s) => s.progress);
  const load = useStore((s) => s.load);
  const select = useStore((s) => s.select);

  useEffect(() => { void load(); }, [load]);

  return (
    <main className="mx-auto max-w-6xl px-6 py-10">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">// road-to-ai</p>
      <h1 className="mt-2 text-4xl font-semibold tracking-tight">Road to AI</h1>
      <p className="mt-4 text-[var(--color-muted)]">
        A gamified, open-source path from math to world models. Click any unlocked node to begin.
      </p>
      <Divider>progress</Divider>
      <ProgressDashboard progress={progress} />
      <Divider>skill tree</Divider>
      <SkillTree levels={levels} onSelect={select} />
    </main>
  );
}
```

- [ ] **Step 4: Smoke test + commit**

```bash
pnpm dev
```

Verify: open the app, complete F00 via the UI, refresh — XP bar shows 1 XP, "first-step" achievement appears.

```bash
git add platform/src/components/AchievementShelf.tsx platform/src/components/ProgressDashboard.tsx platform/src/pages/HomePage.tsx
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): achievement shelf and progress dashboard

HomePage now shows the learner's XP bar, achievements, and summary
stats above the skill tree. Achievement titles render from a local
map keyed by achievement id.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 19: Luminary spotlight + index

**Files:**
- Create: `platform/src/components/LuminarySpotlight.tsx`
- Create: `platform/src/components/LuminariesIndex.tsx`
- Modify: `platform/src/components/LevelPane.tsx`

- [ ] **Step 1: LuminarySpotlight**

The spotlight is the "Luminary spotlight" section already rendered in the level body markdown. This component wraps the `luminary` frontmatter name as a small card above the body content — the body's spotlight section provides the prose.

File: `platform/src/components/LuminarySpotlight.tsx`
```tsx
export function LuminarySpotlight({ name }: { name: string }) {
  const initials = name.split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase();
  return (
    <aside className="mt-6 flex items-center gap-3 rounded border border-[var(--color-border)] bg-[var(--color-surface)] p-3">
      <div className="flex h-10 w-10 items-center justify-center rounded-full border border-[var(--color-accent)] font-mono text-sm text-[var(--color-accent)]">
        {initials}
      </div>
      <div>
        <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">// luminary</p>
        <p className="text-lg font-medium">{name}</p>
      </div>
    </aside>
  );
}
```

- [ ] **Step 2: Insert into LevelPane**

Modify `platform/src/components/LevelPane.tsx` — after the header, before `<Divider>content</Divider>`:

```tsx
import { LuminarySpotlight } from "./LuminarySpotlight";

// inside JSX after the status <p>:
{level.luminary && <LuminarySpotlight name={level.luminary} />}
```

- [ ] **Step 3: LuminariesIndex**

File: `platform/src/components/LuminariesIndex.tsx`
```tsx
import { useMemo } from "react";
import type { Level } from "@/lib/types";
import { Divider } from "./Divider";

export function LuminariesIndex({ levels }: { levels: Level[] }) {
  const byName = useMemo(() => {
    const map = new Map<string, Level[]>();
    for (const l of levels) {
      if (!l.luminary) continue;
      const list = map.get(l.luminary) ?? [];
      list.push(l);
      map.set(l.luminary, list);
    }
    return [...map.entries()].sort(([a], [b]) => a.localeCompare(b));
  }, [levels]);

  return (
    <main className="mx-auto max-w-3xl px-6 py-10">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">// luminaries</p>
      <h1 className="mt-2 text-3xl font-semibold tracking-tight">People you'll meet on the road</h1>
      <Divider>index</Divider>
      <ul className="divide-y divide-[var(--color-border)]">
        {byName.map(([name, entries]) => (
          <li key={name} className="flex items-baseline justify-between py-2">
            <span>{name}</span>
            <span className="font-mono text-xs text-[var(--color-muted)]">
              {entries.map((e) => e.id).join(" · ")}
            </span>
          </li>
        ))}
      </ul>
      {byName.length === 0 && (
        <p className="font-mono text-xs text-[var(--color-muted)]">// no luminaries yet — add a `luminary:` frontmatter field</p>
      )}
    </main>
  );
}
```

- [ ] **Step 4: Route to LuminariesIndex via a simple hash-route**

Modify `App.tsx` to check `location.hash`:
```tsx
import { useEffect, useState } from "react";
import { HomePage } from "./pages/HomePage";
import { LevelPage } from "./pages/LevelPage";
import { LuminariesIndex } from "./components/LuminariesIndex";
import { useStore } from "./store/useStore";

export default function App() {
  const selectedId = useStore((s) => s.selectedId);
  const levels = useStore((s) => s.levels);
  const [hash, setHash] = useState(window.location.hash);
  useEffect(() => {
    const onHash = () => setHash(window.location.hash);
    window.addEventListener("hashchange", onHash);
    return () => window.removeEventListener("hashchange", onHash);
  }, []);

  if (hash === "#/luminaries") return <LuminariesIndex levels={levels} />;

  return (
    <>
      <HomePage />
      {selectedId && <LevelPage />}
    </>
  );
}
```

- [ ] **Step 5: Add a top-bar nav to HomePage**

Before the `<p>//road-to-ai</p>` prefix, add:
```tsx
<nav className="mb-8 flex gap-4 font-mono text-xs uppercase tracking-wide">
  <a href="#/" className="text-[var(--color-accent)]">home</a>
  <a href="#/luminaries" className="text-[var(--color-muted)] hover:text-[var(--color-fg)]">luminaries</a>
  <a href="#/glossary" className="text-[var(--color-muted)] hover:text-[var(--color-fg)]">glossary</a>
  <a href="#/settings" className="text-[var(--color-muted)] hover:text-[var(--color-fg)]">settings</a>
</nav>
```

- [ ] **Step 6: Smoke test + commit**

```bash
pnpm dev
```

Open app → click "luminaries" nav link → sees "S. Keshav — F02". Click back "home" → returns.

```bash
git add platform/src/components/LuminarySpotlight.tsx platform/src/components/LuminariesIndex.tsx platform/src/components/LevelPane.tsx platform/src/App.tsx platform/src/pages/HomePage.tsx
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): luminary spotlight card + /luminaries index page

LuminarySpotlight renders an initials-avatar card at the top of any
level with a `luminary:` frontmatter field. LuminariesIndex aggregates
all luminaries alphabetically with the level IDs they appear in. Simple
hash-based routing (#/luminaries, #/glossary, #/settings) keeps the
app zero-dependency-router for now.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 20: Glossary system

**Files:**
- Create: `curriculum/glossary.md`
- Create: `platform/src/lib/glossary.ts`
- Create: `platform/src/lib/glossary.test.ts`
- Create: `platform/src/components/Glossary.tsx`
- Create: `platform/src/components/GlossaryChip.tsx`
- Create: `platform/src/components/FootnoteList.tsx`
- Modify: `platform/src/components/LevelPane.tsx`
- Modify: `platform/src/App.tsx` (add `#/glossary` route)

- [ ] **Step 1: Seed `curriculum/glossary.md`**

File: `curriculum/glossary.md`
```markdown
# Road to AI Glossary

## XP
**Experience Points.** Awarded on level completion; 1 XP ≈ 1 hour of focused work.

## Track A
**Foundations track.** Textbook + problem-set levels starting from math foundations.

## Track B
**Frontier track.** Paper-reading and reproduction levels, scaffolded for cold-start.

## REPL
**Read-Eval-Print Loop.** Interactive programming environment; Lisp (1958) is the ancestor; Jupyter notebooks are modern descendants.

## SGD
**Stochastic Gradient Descent.** Optimizer that updates parameters using gradients of a mini-batch; the workhorse of deep learning.

## Tier
A grouping of levels sharing a subject (e.g., Tier 1 Math Foundations, Tier 4 Deep Learning). Distinct from XP **tier bands** (Novice, Apprentice, ...).

## Prereq
A level that must be completed before another unlocks. Declared in frontmatter as `prereqs: [F10]`.

## Boss level
A mandatory capstone at the end of each tier that produces a concrete artifact (a trained model, a from-scratch implementation, a reproduction).
```

- [ ] **Step 2: Write glossary loader test**

File: `platform/src/lib/glossary.test.ts`
```ts
import { describe, it, expect } from "vitest";
import { parseGlossary } from "./glossary";

const sample = `# Glossary

## XP
**Experience Points.** A thing.

## SGD
**Stochastic Gradient Descent.** Another thing.
`;

describe("parseGlossary", () => {
  it("splits entries by ## headings", () => {
    const entries = parseGlossary(sample);
    expect(entries).toHaveLength(2);
    expect(entries.find((e) => e.term === "XP")?.body).toContain("Experience Points");
  });
});
```

- [ ] **Step 3: Implement glossary loader**

File: `platform/src/lib/glossary.ts`
```ts
export interface GlossaryEntry {
  term: string;
  body: string;
}

export function parseGlossary(source: string): GlossaryEntry[] {
  const lines = source.split("\n");
  const entries: GlossaryEntry[] = [];
  let current: { term: string; buf: string[] } | null = null;
  for (const line of lines) {
    const m = /^## (.+)$/.exec(line);
    if (m) {
      if (current) entries.push({ term: current.term, body: current.buf.join("\n").trim() });
      current = { term: (m[1] as string).trim(), buf: [] };
      continue;
    }
    if (current) current.buf.push(line);
  }
  if (current) entries.push({ term: current.term, body: current.buf.join("\n").trim() });
  return entries;
}

export async function loadGlossary(): Promise<GlossaryEntry[]> {
  const modules = import.meta.glob("../../../curriculum/glossary.md", {
    eager: true,
    query: "?raw",
    import: "default",
  }) as Record<string, string>;
  const src = Object.values(modules)[0] ?? "";
  return parseGlossary(src);
}
```

- [ ] **Step 4: Glossary index page**

File: `platform/src/components/Glossary.tsx`
```tsx
import { useEffect, useState } from "react";
import { loadGlossary, type GlossaryEntry } from "@/lib/glossary";
import { Divider } from "./Divider";

export function Glossary() {
  const [entries, setEntries] = useState<GlossaryEntry[]>([]);
  useEffect(() => {
    void loadGlossary().then((e) => setEntries(e.sort((a, b) => a.term.localeCompare(b.term))));
  }, []);

  return (
    <main className="mx-auto max-w-3xl px-6 py-10">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">// glossary</p>
      <h1 className="mt-2 text-3xl font-semibold tracking-tight">Glossary</h1>
      <Divider>terms</Divider>
      <dl className="divide-y divide-[var(--color-border)]">
        {entries.map((e) => (
          <div key={e.term} className="py-3">
            <dt className="font-mono text-sm">{e.term}</dt>
            <dd className="mt-1 text-[var(--color-muted)]" dangerouslySetInnerHTML={{ __html: e.body }} />
          </div>
        ))}
      </dl>
    </main>
  );
}
```

- [ ] **Step 5: GlossaryChip (inline hover)**

File: `platform/src/components/GlossaryChip.tsx`
```tsx
import { useState } from "react";
import type { GlossaryEntry } from "@/lib/glossary";

export function GlossaryChip({ entry, children }: { entry: GlossaryEntry; children: React.ReactNode }) {
  const [open, setOpen] = useState(false);
  return (
    <span className="relative inline">
      <button
        onClick={() => setOpen((o) => !o)}
        onBlur={() => setTimeout(() => setOpen(false), 100)}
        className="border-b border-dotted border-[var(--color-accent)] font-mono text-[0.95em]"
      >
        {children}
      </button>
      {open && (
        <span className="absolute bottom-full left-0 z-10 mb-1 w-64 rounded border border-[var(--color-border)] bg-[var(--color-surface)] p-2 text-sm shadow-md">
          <strong className="font-mono text-xs">{entry.term}</strong>
          <span className="mt-1 block" dangerouslySetInnerHTML={{ __html: entry.body }} />
        </span>
      )}
    </span>
  );
}
```

- [ ] **Step 6: Wire glossary route**

In `App.tsx`, add:
```tsx
import { Glossary } from "./components/Glossary";
// …
if (hash === "#/glossary") return <Glossary />;
```

- [ ] **Step 7: FootnoteList**

`remark-gfm` already renders footnotes into the HTML. Component supports a styled bottom-of-pane treatment. For now no extra component is needed; add CSS in globals.css:

```css
@layer components {
  .prose .footnotes {
    margin-top: 3rem;
    padding-top: 1rem;
    border-top: 1px solid var(--color-border);
    font-size: 0.875rem;
    color: var(--color-muted);
  }
  .prose .footnotes::before {
    content: "// footnotes";
    display: block;
    margin-bottom: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
}
```

- [ ] **Step 8: Run tests, smoke test, commit**

```bash
pnpm test:run
pnpm dev  # verify #/glossary shows the 8 starter entries
```

```bash
git add curriculum/glossary.md platform/src/lib/glossary.ts platform/src/lib/glossary.test.ts platform/src/components/Glossary.tsx platform/src/components/GlossaryChip.tsx platform/src/styles/globals.css platform/src/App.tsx
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): glossary loader, index page, inline chip, footnotes styling

curriculum/glossary.md seeds 8 starter entries. parseGlossary splits
the file on ## headings. The /glossary index renders terms with their
body; GlossaryChip is available for future inline auto-linking (will
be enabled on the Level body post-render in the per-level glossary
opt-in path). Footnote section now renders with a "// footnotes"
divider matching the app's code-comment idiom.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 21: Recall system (header + challenges + inline quiz)

**Files:**
- Create: `platform/src/lib/recall.ts`
- Create: `platform/src/components/RecallHeader.tsx`
- Create: `platform/src/components/RecallChallenge.tsx`
- Create: `platform/src/components/LevelQuiz.tsx`
- Modify: `platform/src/components/LevelPane.tsx`
- Modify: `platform/src/store/useStore.ts`

- [ ] **Step 1: Recall helpers**

File: `platform/src/lib/recall.ts`
```ts
import type { Level } from "./types";
import type { Progress } from "./progress";

export function pickRecallLevels(current: Level, allLevels: Level[], progress: Progress, count = 3): Level[] {
  if (current.recall_of?.length) {
    return allLevels.filter((l) => current.recall_of!.includes(l.id)).slice(0, count);
  }
  const done = allLevels.filter((l) =>
    progress.levels[l.id]?.status === "done" &&
    l.track === current.track &&
    l.id !== current.id,
  );
  return done.slice(-count);
}

export function shouldShowChallenge(progress: Progress): boolean {
  const donePerTrack = Object.values(progress.levels).filter((v) => v.status === "done").length;
  // Fire every 5th completion; learner can dismiss per session.
  return donePerTrack > 0 && donePerTrack % 5 === 0;
}
```

- [ ] **Step 2: RecallHeader**

File: `platform/src/components/RecallHeader.tsx`
```tsx
import type { Level } from "@/lib/types";
import { Divider } from "./Divider";

export function RecallHeader({ levels }: { levels: Level[] }) {
  if (!levels.length) return null;
  return (
    <section>
      <Divider>previously on your path</Divider>
      <ul className="space-y-1 font-mono text-xs text-[var(--color-muted)]">
        {levels.map((l) => (
          <li key={l.id}>
            <span className="mr-2 text-[var(--color-accent)]">✓</span>
            {l.id} — {l.title}
          </li>
        ))}
      </ul>
    </section>
  );
}
```

- [ ] **Step 3: LevelQuiz**

File: `platform/src/components/LevelQuiz.tsx`
```tsx
import { useState } from "react";
import type { QuizQuestion } from "@/lib/types";
import { Divider } from "./Divider";

export function LevelQuiz({ questions }: { questions: QuizQuestion[] }) {
  const [answers, setAnswers] = useState<Record<number, string>>({});
  const [revealed, setRevealed] = useState<Record<number, boolean>>({});
  return (
    <section>
      <Divider>quiz</Divider>
      <ol className="space-y-4">
        {questions.map((q, i) => (
          <li key={i}>
            <p className="font-medium">{q.q}</p>
            {q.type === "self-attest" && (
              <div className="mt-1 flex gap-2 font-mono text-xs">
                <button className="rounded border border-[var(--color-border)] px-2 py-1" onClick={() => setAnswers((a) => ({ ...a, [i]: "yes" }))}>
                  yes · i can explain this
                </button>
                <button className="rounded border border-[var(--color-border)] px-2 py-1" onClick={() => setAnswers((a) => ({ ...a, [i]: "no" }))}>
                  not yet
                </button>
                {answers[i] && <span className="ml-2 text-[var(--color-muted)]">recorded</span>}
              </div>
            )}
            {q.type === "multiple-choice" && q.choices && (
              <ul className="mt-1 space-y-1 text-sm">
                {q.choices.map((c, idx) => {
                  const selected = answers[i] === String(idx);
                  const correct = revealed[i] && idx === q.answer;
                  const wrong = revealed[i] && selected && idx !== q.answer;
                  return (
                    <li key={idx}>
                      <button
                        onClick={() => { setAnswers((a) => ({ ...a, [i]: String(idx) })); setRevealed((r) => ({ ...r, [i]: true })); }}
                        className={`w-full rounded border px-2 py-1 text-left ${
                          correct ? "border-[var(--color-accent)] text-[var(--color-accent)]" : wrong ? "border-red-500 text-red-500" : "border-[var(--color-border)]"
                        }`}
                      >
                        {c}
                      </button>
                    </li>
                  );
                })}
              </ul>
            )}
          </li>
        ))}
      </ol>
    </section>
  );
}
```

- [ ] **Step 4: Wire RecallHeader + LevelQuiz into LevelPane**

Modify `LevelPane.tsx` to read store (recall context) and show components:

```tsx
import { useEffect, useState } from "react";
import type { Level } from "@/lib/types";
import { renderMarkdown } from "@/lib/markdown";
import { Divider } from "./Divider";
import { LuminarySpotlight } from "./LuminarySpotlight";
import { RecallHeader } from "./RecallHeader";
import { LevelQuiz } from "./LevelQuiz";
import { useStore } from "@/store/useStore";
import { pickRecallLevels } from "@/lib/recall";

export interface LevelPaneProps {
  level: Level;
  onMarkDone: (id: string) => void;
}

export function LevelPane({ level, onMarkDone }: LevelPaneProps) {
  const [html, setHtml] = useState<string>("");
  const allLevels = useStore((s) => s.levels);
  const progress = useStore((s) => s.progress);
  const recall = pickRecallLevels(level, allLevels, progress);

  useEffect(() => {
    let cancelled = false;
    renderMarkdown(level.body).then((h) => { if (!cancelled) setHtml(h); });
    return () => { cancelled = true; };
  }, [level.body]);

  return (
    <article className="mx-auto max-w-prose px-6 py-10">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">
        {`// ${level.track === "A" ? "foundations" : "frontier"} · tier ${level.tier}`}
      </p>
      <div className="mt-2 flex items-baseline gap-3">
        <span className="font-mono text-sm text-[var(--color-muted)]">{level.id}</span>
        <h1 className="text-3xl font-semibold tracking-tight">{level.title}</h1>
      </div>
      <p className="mt-2 font-mono text-xs text-[var(--color-muted)]">
        {level.xp} XP · {level.status}
      </p>

      {level.luminary && <LuminarySpotlight name={level.luminary} />}
      <RecallHeader levels={recall} />

      <Divider>content</Divider>
      <div
        className="prose prose-invert max-w-none"
        dangerouslySetInnerHTML={{ __html: html }}
      />

      {level.quiz && level.quiz.length > 0 && <LevelQuiz questions={level.quiz} />}

      {level.status !== "done" && (
        <>
          <Divider>action</Divider>
          <button
            onClick={() => onMarkDone(level.id)}
            className="rounded border border-[var(--color-accent)] bg-[var(--color-accent)] px-4 py-2 font-mono text-sm text-[var(--color-accent-fg)] hover:opacity-90"
          >
            Mark done · +{level.xp} XP
          </button>
        </>
      )}
    </article>
  );
}
```

- [ ] **Step 5: RecallChallenge modal (fires after every 5 completions)**

File: `platform/src/components/RecallChallenge.tsx`
```tsx
import { useMemo } from "react";
import type { Level } from "@/lib/types";
import { Divider } from "./Divider";

interface Props {
  recentDone: Level[];
  onClose: () => void;
}

export function RecallChallenge({ recentDone, onClose }: Props) {
  const questions = useMemo(
    () =>
      recentDone.slice(-5).flatMap((l) =>
        l.quiz?.slice(0, 1) ?? [{ q: `Can you still explain "${l.title}" without looking?`, type: "self-attest" as const }],
      ),
    [recentDone],
  );
  return (
    <div className="fixed inset-0 z-20 flex items-center justify-center bg-black/80 p-6">
      <div className="w-full max-w-xl rounded-lg border border-[var(--color-accent)] bg-[var(--color-bg)] p-6">
        <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-accent)]">// recall challenge</p>
        <Divider>checkpoint</Divider>
        <ol className="space-y-3 text-sm">
          {questions.map((q, i) => (
            <li key={i}>{q.q}</li>
          ))}
        </ol>
        <div className="mt-6 flex justify-end gap-2">
          <button onClick={onClose} className="font-mono text-xs text-[var(--color-muted)] hover:text-[var(--color-fg)]">
            skip
          </button>
          <button
            onClick={onClose}
            className="rounded border border-[var(--color-accent)] bg-[var(--color-accent)] px-3 py-1 font-mono text-xs text-[var(--color-accent-fg)]"
          >
            done reflecting
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 6: Fire RecallChallenge from the store**

Modify `useStore.ts` — add `challengeOpen: boolean` state and a `dismissChallenge` action; set `challengeOpen` to true in `markDone` when `shouldShowChallenge(progress)` returns true.

```ts
import { shouldShowChallenge } from "@/lib/recall";

// add to state
challengeOpen: false,
dismissChallenge() { set({ challengeOpen: false }); },

// in markDone, after saveProgress:
const challengeOpen = shouldShowChallenge(progress);
set({ progress, levels, challengeOpen });
```

Then in `App.tsx`, render `<RecallChallenge>` when `challengeOpen` is true, passing in the last few done levels.

- [ ] **Step 7: Run tests + commit**

```bash
pnpm test:run
git add platform/src/lib/recall.ts platform/src/components/RecallHeader.tsx platform/src/components/RecallChallenge.tsx platform/src/components/LevelQuiz.tsx platform/src/components/LevelPane.tsx platform/src/store/useStore.ts platform/src/App.tsx
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): recall system — header, inline quiz, periodic challenge

RecallHeader auto-picks 3 recently-done same-track levels (overridable
via frontmatter recall_of). LevelQuiz renders self-attest and multiple-
choice questions from frontmatter at the bottom of the level pane.
RecallChallenge modal fires every 5th completion, presenting quick
reflections on the last 5 levels.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 22: Selection popover + translate + Settings

**Files:**
- Create: `platform/src/lib/translate.ts`
- Create: `platform/src/components/SelectionPopover.tsx`
- Create: `platform/src/components/Settings.tsx`
- Modify: `platform/src/App.tsx`
- Modify: `platform/src/components/LevelPane.tsx`

- [ ] **Step 1: Settings persistence**

File: `platform/src/lib/translate.ts`
```ts
export type TranslationProvider = "auto" | "browser" | "external" | "api";

export interface SettingsState {
  theme: "dark" | "light" | "system";
  target_language: string;
  translation_provider: TranslationProvider;
  api_key?: string;
  sound: boolean;
}

const KEY = "road-to-ai:settings:v1";

export function loadSettings(): SettingsState {
  try {
    const raw = localStorage.getItem(KEY);
    if (raw) return JSON.parse(raw) as SettingsState;
  } catch { /* ignore */ }
  return {
    theme: "dark",
    target_language: navigator.language.split("-")[0] ?? "en",
    translation_provider: "auto",
    sound: false,
  };
}

export function saveSettings(s: SettingsState): void {
  localStorage.setItem(KEY, JSON.stringify(s));
}

/** Returns true iff the browser exposes the experimental Translation API. */
export function hasBrowserTranslator(): boolean {
  return typeof (self as unknown as { translation?: unknown }).translation !== "undefined";
}

/** Returns null on failure so callers can fall back. */
export async function browserTranslate(text: string, target: string): Promise<string | null> {
  const api = (self as unknown as { translation?: { createTranslator(init: unknown): Promise<{ translate(s: string): Promise<string> }> } }).translation;
  if (!api) return null;
  try {
    const translator = await api.createTranslator({ sourceLanguage: "auto", targetLanguage: target });
    return await translator.translate(text);
  } catch {
    return null;
  }
}

/** Build a Google Translate URL for a given text + target. */
export function googleTranslateUrl(text: string, target: string): string {
  const u = new URL("https://translate.google.com/");
  u.searchParams.set("sl", "auto");
  u.searchParams.set("tl", target);
  u.searchParams.set("text", text);
  u.searchParams.set("op", "translate");
  return u.toString();
}
```

- [ ] **Step 2: SelectionPopover**

File: `platform/src/components/SelectionPopover.tsx`
```tsx
import { useEffect, useState } from "react";
import { browserTranslate, googleTranslateUrl, hasBrowserTranslator, loadSettings } from "@/lib/translate";

export function SelectionPopover() {
  const [text, setText] = useState<string>("");
  const [pos, setPos] = useState<{ x: number; y: number } | null>(null);
  const [translation, setTranslation] = useState<string | null>(null);
  const settings = loadSettings();

  useEffect(() => {
    const onUp = () => {
      const sel = window.getSelection();
      if (!sel || sel.isCollapsed) { setPos(null); setText(""); setTranslation(null); return; }
      const raw = sel.toString().trim();
      if (!raw || raw.length < 2) return;
      const range = sel.getRangeAt(0);
      const rect = range.getBoundingClientRect();
      setText(raw);
      setPos({ x: rect.left + rect.width / 2, y: rect.top });
      setTranslation(null);
    };
    document.addEventListener("mouseup", onUp);
    return () => document.removeEventListener("mouseup", onUp);
  }, []);

  if (!pos || !text) return null;

  const onTranslate = async () => {
    if (settings.translation_provider === "external" || !hasBrowserTranslator()) {
      window.open(googleTranslateUrl(text, settings.target_language), "_blank", "noopener");
      return;
    }
    const t = await browserTranslate(text, settings.target_language);
    setTranslation(t ?? "translation failed · open in Google Translate");
  };

  const onCopy = () => { void navigator.clipboard.writeText(text); };

  return (
    <div
      className="fixed z-30 flex gap-1 rounded border border-[var(--color-border)] bg-[var(--color-surface)] p-1 font-mono text-xs shadow-md"
      style={{ left: pos.x, top: pos.y - 44, transform: "translate(-50%, 0)" }}
    >
      <button onClick={onTranslate} className="rounded px-2 py-1 hover:bg-[var(--color-border)]">
        translate
      </button>
      <button onClick={onCopy} className="rounded px-2 py-1 hover:bg-[var(--color-border)]">copy</button>
      {translation && (
        <div className="absolute top-full mt-1 max-w-xs rounded border border-[var(--color-border)] bg-[var(--color-bg)] p-2 text-sm">
          {translation}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: Settings page**

File: `platform/src/components/Settings.tsx`
```tsx
import { useState } from "react";
import { loadSettings, saveSettings, type SettingsState } from "@/lib/translate";
import { Divider } from "./Divider";

const LANGUAGES = [
  ["en", "English"], ["zh", "中文 (Chinese)"], ["ja", "日本語 (Japanese)"],
  ["ko", "한국어 (Korean)"], ["es", "Español"], ["fr", "Français"],
  ["de", "Deutsch"], ["pt", "Português"], ["ru", "Русский"], ["ar", "العربية"],
];

export function Settings() {
  const [s, setS] = useState<SettingsState>(loadSettings());
  const update = (partial: Partial<SettingsState>) => {
    const next = { ...s, ...partial };
    setS(next);
    saveSettings(next);
    if (partial.theme) {
      document.documentElement.classList.remove("dark", "light");
      document.documentElement.classList.add(partial.theme === "system" ? "" : partial.theme);
    }
  };
  return (
    <main className="mx-auto max-w-xl px-6 py-10">
      <p className="font-mono text-xs uppercase tracking-wide text-[var(--color-muted)]">// settings</p>
      <h1 className="mt-2 text-3xl font-semibold tracking-tight">Settings</h1>

      <Divider>appearance</Divider>
      <label className="block text-sm">theme
        <select value={s.theme} onChange={(e) => update({ theme: e.target.value as "dark" | "light" | "system" })} className="ml-2 rounded border border-[var(--color-border)] bg-transparent p-1 font-mono text-xs">
          <option value="dark">dark (default)</option>
          <option value="light">light</option>
          <option value="system">system</option>
        </select>
      </label>

      <Divider>translation</Divider>
      <label className="block text-sm">target language
        <select value={s.target_language} onChange={(e) => update({ target_language: e.target.value })} className="ml-2 rounded border border-[var(--color-border)] bg-transparent p-1 font-mono text-xs">
          {LANGUAGES.map(([code, name]) => <option key={code} value={code}>{name}</option>)}
        </select>
      </label>
      <label className="mt-3 block text-sm">provider
        <select value={s.translation_provider} onChange={(e) => update({ translation_provider: e.target.value as SettingsState["translation_provider"] })} className="ml-2 rounded border border-[var(--color-border)] bg-transparent p-1 font-mono text-xs">
          <option value="auto">auto · browser if available, else external</option>
          <option value="browser">browser only</option>
          <option value="external">always open Google Translate</option>
          <option value="api">api key (advanced)</option>
        </select>
      </label>

      <Divider>sound</Divider>
      <label className="flex items-center gap-2 text-sm">
        <input type="checkbox" checked={s.sound} onChange={(e) => update({ sound: e.target.checked })} />
        enable completion chime (default off)
      </label>
    </main>
  );
}
```

- [ ] **Step 4: Route Settings and mount SelectionPopover**

In `App.tsx`:
```tsx
import { Settings } from "./components/Settings";
import { SelectionPopover } from "./components/SelectionPopover";

// in the return:
if (hash === "#/settings") return <><Settings /><SelectionPopover /></>;

// and always mount SelectionPopover alongside HomePage/LevelPage
return (
  <>
    <HomePage />
    {selectedId && <LevelPage />}
    <SelectionPopover />
  </>
);
```

- [ ] **Step 5: Commit**

```bash
pnpm test:run
git add platform/src/lib/translate.ts platform/src/components/SelectionPopover.tsx platform/src/components/Settings.tsx platform/src/App.tsx
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): selection-based translate popover + settings page

SelectionPopover mounts on mouse-up and offers translate/copy actions
on any highlighted text. Translation path prefers the browser's
Translation API (Chrome 131+) and falls back to an external Google
Translate link. Settings page persists theme, target language,
provider preference, and completion-chime toggle to localStorage.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 23: M3 milestone tag and deploy

- [ ] **Step 1: Update CHANGELOG, tag, push**

Append to `CHANGELOG.md`:
```markdown
## [platform-v0.2.0] — 2026-04-15

### Added
- Progress persistence via localStorage; tier computed from XP total
- XpBar, TierBadge, AchievementShelf, ProgressDashboard
- Achievements engine with 7 starter rules (first-step, luminary breadth, etc.)
- Luminary spotlight card + /luminaries index page
- Glossary system: curriculum/glossary.md + /glossary index + inline chip
- Footnotes with "// footnotes" styling
- Recall system: auto-recall header, inline quiz, periodic Recall Challenge every 5 levels
- Highlight-to-translate popover with browser Translation API and Google Translate fallback
- Settings page: theme (dark default), target language, translation provider, sound toggle
```

```bash
git add CHANGELOG.md
git -c commit.gpgsign=false commit -m "docs: log platform-v0.2.0 M3 milestone"
git tag -a platform-v0.2.0 -m "M3: gamification core"
git push origin main
git push origin platform-v0.2.0
gh run watch
```

Open live URL, complete F00 end-to-end, verify XP/achievement/popover all work.

**M3 done.**

---

## M4 — Flagship content + polish

**Outcome:** First 15 Track A levels authored; CLAUDE.md + slash commands authored; mobile-responsive; landing polished; live URL is a working app for any learner.

### Task 24: Author levels F03–F06 (Prelude complete + Math Tier 1 start)

Author each of these four level files following the authoring rules in `curriculum/README.md`. Each file must include: correct frontmatter (id, title, track A, tier, xp, prereqs, unlocks, status: pending), a "Why this level matters (lineage)" section, objectives, resources, tasks, done criteria, and a "Bridge to modern" section. At least 2 of the 4 must include a luminary spotlight; all must include ≥1 visual element.

Content direction (not full text — engineer authors following the spec):

- [ ] **Step 1: F03 — The Python numerical stack (tier 0, 2 XP)**
  - Prereqs: `[F02]`. Unlocks: `[F04]`.
  - Covers: numpy, matplotlib, torch quickstart. Classical root: APL / S-PLUS. Modern descendant: numpy as the substrate under all of modern ML.
  - Luminary: **Travis Oliphant** (creator of NumPy). 3–5 sentences covering his role in numpy/SciPy, founding of Anaconda, and one personal detail.
  - Visual: a mermaid diagram showing "array → tensor → autograd tensor".
  - Commit: `feat(curriculum): add F03 — python numerical stack`

- [ ] **Step 2: F04 — Vectors and spaces (tier 1, 5 XP)**
  - Prereqs: `[F03]`. Unlocks: `[F05, F06]`.
  - Covers: vectors, linear combinations, spans, bases. Classical root: Grassmann (1844). Modern descendant: embedding spaces in LLMs.
  - Luminary: **Hermann Grassmann**.
  - Visual: a KaTeX equation showing a linear combination + a small 2D vector diagram as Mermaid or embedded SVG.
  - Resources: Deisenroth *MML* §2.1–2.4, 3Blue1Brown "Essence of Linear Algebra" E1–E3.
  - Commit.

- [ ] **Step 3: F05 — Matrices as transformations (tier 1, 6 XP)**
  - Prereqs: `[F04]`. Unlocks: `[F07, F08]`.
  - Covers: matrix-vector product, geometric intuition, matrix multiplication as composition. Classical root: Cayley & Sylvester (1858). Modern descendant: transformer `Q @ K^T / sqrt(d)` attention computation.
  - Visual: mermaid state diagram showing `vector → matrix_apply → vector'`.
  - Quiz (self-attest + multiple choice, 3 questions).
  - Commit.

- [ ] **Step 4: F06 — Dot products, norms, and projections (tier 1, 5 XP)**
  - Prereqs: `[F04]`. Unlocks: `[F07]`.
  - Covers: dot product geometry, cosine similarity, Cauchy-Schwarz. Classical root: Hamilton (1840s quaternion / inner product). Modern descendant: cosine similarity in embedding search / RAG.
  - Luminary: **William Rowan Hamilton**.
  - Commit.

---

### Task 25: Author levels F07–F10 (linear algebra depth)

- [ ] **Step 1: F07 — Linear independence and rank (tier 1, 5 XP)**
  - Lineage: column space → PCA → modern representation learning.
  - Commit.

- [ ] **Step 2: F08 — Solving linear systems (tier 1, 5 XP)**
  - Classical: Gaussian elimination (Gauss, 1810). Modern: the least-squares subroutine inside every regression library.
  - Luminary: **Carl Friedrich Gauss**.
  - Commit.

- [ ] **Step 3: F09 — Eigenvalues and eigenvectors (tier 1, 6 XP)**
  - Classical: Cauchy-Schwarz → Sylvester's signature theorem. Modern: PCA, spectral clustering, Google's PageRank.
  - Visual: a 2D diagram showing an eigen-decomposition of a simple shear matrix.
  - Commit.

- [ ] **Step 4: F10 — Functions, continuity, limits (tier 1, 4 XP, prereqs: [F06])**
  - Sets up the calc thread.
  - Luminary: **Karl Weierstrass** (rigorous ε-δ).
  - Commit.

---

### Task 26: Author levels F11–F14 (calculus → gradient descent)

- [ ] **Step 1: F11 — Derivatives and chain rule (tier 1, 5 XP)**
  - Classical: Leibniz/Newton. Modern: autodiff.
  - Luminary: **Gottfried Leibniz**.
  - Commit.

- [ ] **Step 2: F12 — Multivariable calculus and gradients (tier 1, 6 XP)**
  - Classical: Lagrange's mécanique analytique. Modern: gradient of a loss.
  - Commit.

- [ ] **Step 3: F13 — Loss surfaces and critical points (tier 1, 4 XP)**
  - Visual: Mermaid of a simple paraboloid loss surface; KaTeX for gradient conditions.
  - Commit.

- [ ] **Step 4: F14 — Gradient descent intuition (tier 1, 6 XP)** *(the exemplar from the spec)*
  - Full luminary: **Augustin-Louis Cauchy** (as the spec shows in §6.C).
  - glossary_terms: `[SGD, learning-rate, loss-surface]`
  - quiz (3 questions, one self-attest, one multiple-choice).
  - Include the actual `gd.py` code block (shown highlighted by Shiki):

```python
import numpy as np
import matplotlib.pyplot as plt

def f(x, y):
    return x ** 2 + 10 * y ** 2

def grad(x, y):
    return np.array([2 * x, 20 * y])

def descent(lr=0.1, steps=40, start=(5.0, 3.0)):
    path = [np.array(start)]
    for _ in range(steps):
        p = path[-1]
        path.append(p - lr * grad(*p))
    return np.array(path)

path = descent(lr=0.1)
plt.plot(path[:, 0], path[:, 1], marker="o")
plt.title("GD on f(x,y) = x^2 + 10y^2")
plt.show()
```

  - Mermaid diagram showing the descent loop.
  - Commit.

---

### Task 27: CLAUDE.md and slash commands

**Files:**
- Create: `CLAUDE.md`
- Create: `.claude/commands/status.md`
- Create: `.claude/commands/lesson.md`
- Create: `.claude/commands/level-up.md`
- Create: `.claude/commands/paper-week.md`
- Create: `.claude/commands/review.md`
- Create: `.claude/commands/tree.md`
- Create: `.claude/commands/new-level.md`

- [ ] **Step 1: Write root `CLAUDE.md`**

File: `CLAUDE.md`
```markdown
# Road to AI — Claude Companion Rules

> Authored by Prof. Claude, with Xavier Loo as co-author and first learner.
> You — the Claude session reading this — are Prof. Claude on duty. Act accordingly.

This repo is a gamified, cold-start AI curriculum. When Claude assists a learner in this directory:

## Tone

- Treat the learner as a cold-start programmer with rusty math. Ground jargon, explain notation, show worked examples.
- Every response involving a classical idea must name ≥1 modern descendant. Every response involving a modern idea must trace ≥1 classical root.
- Never suggest streaks, daily quotas, or other time-pressure mechanics.

## File contract

- Markdown under `curriculum/` is the source of truth. Claude writes; the app reads.
- Write progress updates via `progress.json` or the `/level-up` slash command — never in-memory only.
- Never translate highlighted text silently — wait for explicit `/translate` invocation or the user's UI action.

## Curriculum authoring rules

- Levels: `curriculum/levels/track-a/<ID>_<slug>.md`. Required frontmatter: id, title, track, tier, xp, prereqs, unlocks, status, completed_at.
- Every level must include a "Why this level matters (lineage)" section.
- Luminary spotlights are 3–5 sentences, mix classical and modern figures.
- Glossary entries go in `curriculum/glossary.md`; keep each ≤3 sentences.
- Visual ≥1 per level: Mermaid diagram, KaTeX equation, image, or code block.

## Commands

Available slash commands live in `.claude/commands/`:

- `/status` — summarize current position on both tracks, XP, achievements
- `/lesson` — open the current in-progress level
- `/level-up <id>` — mark a level done, update progress.json, award XP, check achievements
- `/paper-week` — alias for /lesson targeting the current Track B level
- `/review` — pick a random completed level for a spaced-repetition quiz
- `/tree` — print the skill tree with completion state
- `/new-level <id>` — scaffold a new level file from the template
```

- [ ] **Step 2: Write each slash command file**

Each file in `.claude/commands/` is a short markdown playbook. Example template — repeat with appropriate content for each:

File: `.claude/commands/status.md`
```markdown
---
description: Show current curriculum progress
---

Read `progress.json` at repo root (or report if missing) and report:

1. **Track A current:** ID + title of the lowest-ID level whose `status == "in-progress"`, or the next unlocked `"pending"` level.
2. **Track B current:** same, but filtered to `track: B`.
3. **XP:** `progress.xp_total`.
4. **Tier:** `progress.tier`.
5. **Recent achievements:** up to 3 most recent from `progress.achievements`.
6. **Suggested next session:** 1 sentence recommending A or B based on which is further behind.

Render in a compact block using `//` dividers.
```

File: `.claude/commands/level-up.md`
```markdown
---
description: Mark a level done, update progress, award XP, check achievements
arguments: "<level-id>"
---

Given a level id (e.g. `F14`):

1. Find the level in `curriculum/levels/track-a/` or `track-b/`.
2. Read `progress.json` (or create default shape if missing).
3. If level already done, stop and report.
4. Update `progress.levels[id] = { status: "done", completed_at: <today> }`.
5. Add `level.xp` to `progress.xp_total`.
6. Recompute `progress.tier` from tier-bands in `curriculum/meta.json`.
7. Run achievement rules; append any new awards.
8. Write `progress.json`.
9. Report: XP gained, new tier (if changed), achievements earned, newly-unlocked level IDs.

Never use `git add -A` — add only `progress.json`.
```

File: `.claude/commands/lesson.md`
```markdown
---
description: Open and guide through the current in-progress level
---

1. Load progress to find the current in-progress level (or the next pending unlocked level).
2. Open its markdown file.
3. Summarize in one paragraph WHY it matters (lineage section).
4. Walk through the objectives and tasks, asking clarifying questions when appropriate.
5. Never mark the level done automatically — wait for the user to explicitly `/level-up`.
```

File: `.claude/commands/paper-week.md`
```markdown
---
description: Jump into the current Track B (frontier) level
---

Run the `lesson` playbook but filter to levels where `track: B` and `status` is `pending` or `in-progress`.
```

File: `.claude/commands/review.md`
```markdown
---
description: Spaced-repetition quiz on a random completed level
---

1. From `progress.levels` pick a random id whose status is "done".
2. Open its markdown file and extract the "Done criteria" section.
3. Without revealing the criteria verbatim, ask 2–3 questions that probe whether the learner still meets them.
4. After the learner answers, reveal the criteria, and give them a self-attest yes/no.
5. If yes, do nothing. If no, reset the level's status to "in-progress" in progress.json.
```

File: `.claude/commands/tree.md`
```markdown
---
description: Print the skill tree with completion state
---

Render an ASCII tree of all levels under `curriculum/levels/` showing:

- ✓ done (accent)
- ◐ in progress
- ○ unlocked, pending
- · locked

Group by track (A, B). Group A by tier. Show level id and title. Keep under 80 columns.
```

File: `.claude/commands/new-level.md`
```markdown
---
description: Scaffold a new level file from the template
arguments: "<id> <slug>"
---

Create `curriculum/levels/track-a/<ID>_<slug>.md` (or track-b/ if id starts with "P"):

Use this template, replacing `<...>` placeholders:

    ---
    id: <ID>
    title: <Title>
    track: <A|B>
    tier: <0-8>
    xp: <estimated hours>
    prereqs: []
    unlocks: []
    status: pending
    completed_at: null
    tags: []
    luminary: null
    glossary_terms: []
    ---

    ## Why this level matters (lineage)
    Classical root: <...>
    Modern descendants: <...>

    ## Objectives
    - <...>

    ## Resources
    - <...>

    ## Tasks
    - [ ] <...>

    ## Done criteria
    <...>

    ## Bridge to modern
    <...>

After creating the file, remind the user to update the prereq level's `unlocks: []` field to include this new level's id.
```

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md .claude/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(claude): add CLAUDE.md and 7 slash commands for learner companion

CLAUDE.md sets the tone (cold-start learner, ground jargon, always
connect classical to modern, no time-pressure mechanics) and defines
the file contract (markdown source of truth; Claude writes, app reads).
Seven slash commands under .claude/commands/ cover status, lesson,
level-up, paper-week, review, tree, and new-level.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 28: Mobile responsive pass

- [ ] **Step 1: Inspect breakpoints**

Run `pnpm dev`. Open the app in Chrome DevTools, toggle Device Mode, test at 375px (iPhone SE), 768px (iPad), and 1280px (laptop).

- [ ] **Step 2: Fix identified issues**

Likely fixes:
- SkillTree container: `h-[70vh]` may be too tall on mobile. Change to `h-[50vh] md:h-[70vh]`.
- HomePage padding: `px-6` is fine. Hero heading `text-4xl` on mobile reduces readability of wrapped lines — use `text-3xl md:text-4xl`.
- LevelPane modal: takes full viewport on mobile instead of centered card — check `max-w-3xl` vs available width; add `w-full h-full md:w-auto md:h-auto`.
- Settings page `grid-cols-3` on dashboard → stack on mobile with `grid-cols-1 md:grid-cols-3`.
- Navigation links: `mb-8 flex gap-4` → stays fine, but add overflow handling.

Commit small per-component adjustments.

- [ ] **Step 3: Commit**

```bash
git add platform/src/
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
chore(platform): mobile-responsive refinements

Skill tree height, hero size, dashboard grid, and level-pane modal
now adapt to mobile viewports (375px+). Navigation unchanged.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 29: Landing page polish and fork-me messaging

- [ ] **Step 1: Expand HomePage hero**

When no level is selected and total XP is 0, show extra onboarding:
- A one-paragraph "What is this" pitch
- A "Fork this repo" block with a copy-pasteable command
- A single big "Start with F00" button that selects the first level

Modify `HomePage.tsx` to conditionally render this block when `progress.xp_total === 0`.

- [ ] **Step 2: Update root README with a screenshot**

Take a dark-mode screenshot of the live app. Commit it to `docs/screenshots/hero-dark.png` and reference it in `README.md`:
```markdown
![Road to AI dark mode](docs/screenshots/hero-dark.png)
```

- [ ] **Step 3: Commit**

```bash
git add platform/src/pages/HomePage.tsx docs/screenshots/ README.md
git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
feat(platform): landing-page polish and fork-me block for new users

HomePage shows an expanded hero and fork instructions when xp_total
is 0, letting a first-time visitor start F00 with one click. README
now includes a hero screenshot for GitHub previews.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 30: Visual parity check vs xavierloo.com and final release

- [ ] **Step 1: Side-by-side visual audit**

Open `https://xavierloo.com` in one browser window and `https://jvloo.github.io/road-to-ai/` in another. Compare:

| Element | Check |
|---|---|
| Body background | Matches |
| Body text color | Matches |
| Monospace for dividers | Matches |
| Hero heading weight and line-height | Matches |
| Accent color (LIVE dot vs current-level dot) | Matches |
| Spacing scale | Feels the same |
| Link underline style | Matches |

Record any mismatches in `docs/design-decisions.md` and fix in `tokens.css`.

- [ ] **Step 2: Run the full test suite**

```bash
pnpm test:run
pnpm typecheck
pnpm build
```

Expected: all tests pass, no type errors, clean build.

- [ ] **Step 3: Update CHANGELOG, tag, and release**

Append to `CHANGELOG.md`:
```markdown
## [platform-v0.3.0] — 2026-04-15

### Added
- First 15 Track A levels (F00–F14) authored with luminary spotlights and lineage
- curriculum/glossary.md seeded with 15+ entries
- Root CLAUDE.md + 7 slash commands under .claude/commands/
- Mobile-responsive layout across home, level pane, settings, luminaries, glossary
- Landing hero with fork-me block for new users
- Hero screenshot in README
- Side-by-side visual audit against xavierloo.com; tokens.css reconciled
```

Commit, tag, push:
```bash
git add CHANGELOG.md docs/design-decisions.md platform/src/styles/tokens.css
git -c commit.gpgsign=false commit -m "docs: log platform-v0.3.0 M4 milestone"
git tag -a platform-v0.3.0 -m "M4: flagship content + polish"
git push origin main
git push origin platform-v0.3.0
gh run watch
```

Open the live URL on desktop AND mobile. Complete F00 end-to-end. Verify:
- Dark mode on first load
- Code-comment dividers present and consistent
- F02 shows the S. Keshav luminary card
- Glossary page renders
- A text selection → translate popover
- Settings page lets you switch language and theme
- Recall Challenge fires after 5 level completions (can simulate by manually editing progress.json)

- [ ] **Step 4: Cut a GitHub Release**

```bash
gh release create platform-v0.3.0 \
  --title "Road to AI — M4: Flagship content shipped" \
  --notes-file CHANGELOG.md \
  --latest
```

**M4 done.** Road to AI is live, public, dark by default, and ready for its first learner — you.

---

## Appendix A — Running the plan

**Recommended sequence:** follow tasks 1 → 30 in order. Each task ends in a green test run and a commit. Between milestones (M1→M2→M3→M4), push to GitHub and watch the Pages workflow deploy before starting the next milestone.

**Checkpoints for review:**
- After Task 6 (M1 complete): live URL renders placeholder; verify deployment works.
- After Task 14 (M2 complete): click a level, see it render with full markdown features.
- After Task 23 (M3 complete): complete a level, XP updates, translate works.
- After Task 30 (M4 complete): full release tagged, README polished, learner can begin.

**When tests fail:** do NOT skip ahead. Fix the failing test before committing. If the test is wrong, fix the test; if the code is wrong, fix the code. Never commit red tests.

**When uncertain about authoring a level (Tasks 24–26):** the spec at `docs/superpowers/specs/2026-04-15-road-to-ai-curriculum-and-platform-design.md` §6.C shows F14 as the exemplar. Every level should feel like that.

## Appendix B — What's NOT in this plan (v2 backlog)

Parked for post-M4 work:
- Track B starter levels (P01–P05) — M5
- Full tier-coloring of the skill tree
- Obsidian plugin for level authoring
- CLI tool for level scaffolding outside Claude Code
- Spaced-repetition engine beyond the basic `/review` command
- Community curriculum directory (other forks)
- Interactive widget islands (e.g., GD visualizer)
- DeepL / OpenAI API-key translation path implementation
- Completion chime sound asset

Add any further features here rather than expanding M1–M4.
